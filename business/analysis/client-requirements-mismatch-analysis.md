# Client Requirements vs Current Blueprint Mismatch Deep Analysis (v0.1)

**Task:** "Client Requirements vs Current Blueprint Mismatch Deep Analysis" (parallel to ongoing ANALYZE-*; Chief Staff Engineer + Critical Analyst subagent).

**Date:** 2026-06-09  
**Status:** COMPLETE (full cycle executed; report delivered to mandated path; feeds SYNTH-001 + blueprint updates before final SYNTH).  
**Grounding:** 
- New client req: verbatim from query "Описание сайта" block (structure marketplace/quest/constructor; technical gameplay via smartphone; sequence of web pages; game step definition; 2 task types (no answer: move+optional action e.g. statue hands; with answer: enter correct e.g. knowledge/plaque/count/rebus); 4 templates (First screen, Task no answer, Task with answer, Continue); graphic content (comic-like image per page for task/character/hint/atmosphere); navigator button for location tasks (optional hint); bonuses (coins for tasks, animated/voiced, spend on hints or for rating); hints (via wrong answer popup, exchange coins); feedback (report errors from menu any page "Оставить отзыв"; rate/comment after completion)).
- All prior completed ANALYZE reports + blueprint sections (GameStep/gamestep-completion-variants.md + review-gamestep.md, Coins/coins-economy-variants.md + review-coins.md, Constructor/constructor-authoring-variants.md + review-constructor.md, Offline/offline-model-variants.md + review-offline.md, Progress/Sync/progress-attempt-sync-variants.md + sync-recording-variants.md, FINAL-BEST-PRACTICE-BLUEPRINT.md, SYNTHESIS-NOTE-GAMESTEP-COINS-CONSTRUCTOR.md, others in analysis/).
- Locked in business/08_DECISIONS_LOG.md (Physical uniform confirm "no difference"/no subtypes v1; Coins earned exclusively gifts/completions from snapshot, spend hints only, no admin/real$ v1; multiline answers; save+test preview acceptable; explicit publish snapshots; linear; client full local val no re-val; etc.) + business/01_DOMAIN_MODEL... (GameStep: 2 primary modes physical/answer + rich supporting composable gift/hint/media/terminal/narrative_advance; Review thin post-completion; no navigator/comic/rating-coins/any-page feedback/marketplace-as-3rd/4-hard-templates), 03_OFFLINE... (PWA bundle full GameStep+content+answers list+geo; physical explicit confirm; hints coin per-step reveal; sync records facts + local outcomes + coin recon; mutable attempt + last-write/union for multi-dev; ~5MB), 04_CONTENT... (GameStep atomic; 2 task types + supporting; constructor form+list+multiline+publish+save+test; rich content primary differentiator; no 14 Page_types), 06_V1... (catalog/discovery + player + constructor; post-completion review; coins for hints; no explicit 4 templates/navigator/comics/any-page feedback/coins-for-rating), 07_ASSUMPTIONS... (physical confirm honesty; coins only gifts/completions; linear; "save+test" sufficient; no real$).
- Discovery/parsed (old page_constructor 36 fields + 14 Page_type incl. questionnoanswer/question/gift/lead/congratulations/... + Answer list + Gift_Coins; 556 steps/21 quests; 1163 WFs as cautionary; no "comic"/"navigator"/"Оставить отзыв" concepts).
- Extreme skepticism throughout: every client point attacked for edges; current blueprint attacked for violations/ignores of client; resolutions mini-cycled (deconstruct edges, expose flaws, rebuild better, self-crit); no sacred cows (even locked "uniform physical" or "supporting richer" or "thin review" must yield or be called mismatch); ground only in sources + new req; max docs (tables, cross-impacts, risks, invariants, status+path).

**Process Executed (full cycle as mandated):**
1. Deconstruct new client req: list all specific points + edge cases (e.g., action in physical can't be "confirmed" digitally? navigator optional but UX impact? rating from coins - global or per-quest? comic images - per-step or reusable across? templates - hard 4 or configurable? marketplace integration with constructor? feedback as bug report vs rating? voiced/animated - client or server? sequence of *web* pages on smartphone PWA?).
2. Compare point-by-point to current locked + all prior analyses/recommendations (ANALYZE-01/03/04/05/07 + reviews + FINAL + SYNTHESIS + business/01/03/04/06/07/08 + discovery counts). Exact mismatches called (e.g., GameStep had "supporting" beyond 4 templates; physical "confirm" but client "move + sometimes perform action"; coins for rating *new*; navigator *new*; feedback details *new and broader*; explicit 3-component structure; comic per-page *new*; hints via wrong-popup *new*; animated/voiced *new*).
3. Expose flaws harshly in current blueprint vs this req (e.g., ignored "navigator" or "comic" or "rating" or "any-page feedback" or "4 templates" violates client; our "uniform physical" may not capture "perform action" example given in client + docs; templates vs our richer supporting (narrative/gift/media/terminal/hint as first-class); no mention of marketplace-as-top-level in some slices (catalog is thin); Progress/Sync (ANALYZE-05) mutable+union does not address template-specific events or navigator state; Offline bundle docs focus on geo/hint but silent on comic images/navigator data; Constructor supports rich but not mapped to hard 4 or comic assignment or navigator config; Coins locked "hints only" + "no rating link" + no animated/voiced).
4. Rebuild: propose resolutions/adjustments for *each* mismatch (map 4 templates to GameStep + content; enhance physical to support "action" via rich content or optional subtypes/flag in supporting *without* violating uniform confirm mechanism or locked "no difference"; add rating as player stat from coin accumulation or separate spend path; add navigator as optional supporting behavior + data in location steps; ensure constructor supports these exact templates + comic image assignment (roles: task/character/hint/atmosphere) + navigator config; integrate marketplace as catalog + grants discovery surface that lists constructor-published quests; feedback as thin review + bug report entity (from any page context + step); hints trigger via wrong-answer popup + exchange).
5. For each resolution: apply mini cycle (deconstruct its edges, expose flaws in proposal, rebuild better, self-critique).
6. Synthesize: updated invariants; impacts on *all* slices (e.g., GameStep must support the 2 task types + 4 templates *as primary* (richer supporting ok if backward); Coins must track for rating + animated/voiced events?; Progress/Sync must log events for these steps + navigator/hint-popup flows; Offline bundle *must* include navigator data if any, comic images (per-page); Constructor must have UI for these templates + image assignment + navigator config + feedback config?).
7. Output this comprehensive report (mandated path) with point-by-point table (client point | current blueprint | mismatch? | resolution | why chosen | cross impacts | risks). Status + path at end.

**Deconstruction of New Client Req (Specific Points + Edge Cases, Skeptical):**
From query "Описание сайта" block (treated as authoritative new input overriding or augmenting priors where conflict):
- **Overall structure:** Site/app has 3 explicit top-level components/sections: marketplace (discovery/purchase/catalog of quests), quest (the player gameplay experience), constructor (authoring for internal admins).
- **Technical:** Gameplay is via smartphone (PWA/mobile-first implied); quests are a *sequence of web pages* (rendered steps/UI).
- **Game step definition:** Core unit is "game step"; quests = ordered sequence.
- **2 task types (primary, exhaustive for tasks):**
  - No answer required: "move + optional action" (e.g., "go to location, touch the hands of the statue" – physical world action that may not have digital "proof"; player "does" it then advances).
  - With answer: "enter correct" (knowledge question, text from plaque, count something, solve rebus/puzzle; exact match expected).
- **4 templates (hard? or primary UI skins for steps/pages):** First screen (start/greeting), Task no answer (physical/move+action), Task with answer (question/input), Continue (narrative bridge/advance).
- **Graphic content:** Comic-like (style/illustrated) image *per page* (per step), with roles/purposes: for task (main visual), character (NPC/guide?), hint (revealed?), atmosphere (background/immersion). Not generic photos; comic aesthetic.
- **Navigator button:** For location tasks (physical/no-answer); optional; acts as hint? (shows location/nav?); tied to geo.
- **Bonuses/coins:** Coins awarded for (completing?) tasks; *animated and voiced* (delight/feedback); spend options: on hints *or* "for rating" (to boost/complete rating? unlock rating ability? spend to improve post-quest rating?).
- **Hints:** Triggered via wrong answer popup (for answer-type tasks); exchange/spend coins for reveal.
- **Feedback:** "Оставить отзыв" (leave review/feedback) accessible from *menu on any page* (not just post-complete); used to *report errors/bugs* (from any step/context). Separately, *rate + comment after quest completion*.
- **Other implied:** Smartphone sequence implies linear web-like paging with back/forward? PWA offline? (cross to priors); marketplace integrates quests from constructor (publish → appear in market); constructor produces the templated steps with comics/nav/etc.

**Edge cases (harsh, from deconstruct):**
- Physical "move + optional action" (statue hands): What if action can't be digitally confirmed (no photo proof per locked v1; trust honesty like current)? Does "optional action" require UI variant or just rich content + confirm button? If player lies about "touching", same as uniform confirm.
- Navigator optional: If present, affects UX flow (shows map/nav inline?); if absent for a location task, pure text+confirm? Optional per-step or per-quest? Affects offline bundle (must ship nav data? map assets?).
- Rating from coins: Global player rating score derived from total coins ever? Per-quest? Spend coins *to submit a rating* (gated)? Or spend to "improve" rating (pay for stars)? Per-quest spend or lifetime? Animated/voiced on award – client-only or needs asset in bundle?
- Comic images per page: One primary comic per template/step? Or multiple (task/character/hint/atmosphere roles – 4 images/step?)? Reusable across steps/quests or unique? Size impact on ~5MB bundle (comics can be heavy; PWA cache). Offline must include them.
- 4 templates hard vs configurable: Are these the *only* allowed step UIs (no pure gift/media/terminal outside these)? Or base 4 + supporting (e.g. gift on "Continue" or "Task")? Constructor must enforce exactly these 4 or map richer to them?
- Marketplace how integrates with constructor: Publish in constructor → instantly/after approval in marketplace catalog? Pricing/visibility from constructor? Grants/purchases flow through marketplace surface?
- Hints via wrong popup: Only on answer tasks after wrong submit? Cost fixed or per-step? Exchange = spend coins to see answer/hint? Affects "wrong answer count" + coins tracking.
- Feedback any-page "Оставить отзыв": Menu (global nav?) on *every* step/page, even mid-quest; context (current step/quest/attempt) auto-attached for bug reports. Separate from post-completion rate/comment (which is Review entity). Errors vs general feedback? Moderation?
- Sequence of *web pages* on smartphone: Implies browser-like or PWA with page transitions; back button? Progress indicator? Affects player state machine + sync.
- Animated/voiced bonuses: Requires audio assets (voiced) + animation defs in step content or global? Bundle size + offline play of media.
- Coins for tasks: Award on complete of task step (physical or answer), or only some? First-only or per-attempt? Ties to "bonuses for tasks".
- General: Smartphone implies touch/ geo / offline primary; "web pages" may conflict with native-feel PWA if too literal. No branching mentioned (aligns locked linear). Russian assumed.

**Cross-Reference to Prior + Locked (Point-by-Point Mismatches Exposed):**
Priors/locked (from reads of 08/01/03/04/06/07 + ANALYZE-03/04/05/07 + reviews + FINAL/SYNTHESIS + discovery):
- GameStep: Exactly 2 primary task types (physical/no-answer uniform explicit "I did it"/confirm + optional note; answer list strings + client match). *Richer supporting* (gift coins/narrative, hint cost+reveal geo/content, video, terminal+review_prompt, narrative_advance/Continue, is_start, media). Rich content primary for atmosphere/differentiation (addresses "uniform physical reduces magic"). *No 4 hard templates*; supporting composable (not limited). Physical example in docs *exactly* "touch the cold metal hands of the statue" (from 04/00/03) – matches client, but model is *uniform confirm mechanism* ("no difference" locked in 08: "we do not need subtypes or different completion UIs for v1 physical tasks"; "rich surrounding content provides differentiation").
- Coins: Earned *exclusively* via quest completions + gifts defined in GameSteps (snapshot-frozen amounts); spend *only on hints* (per-step coin-gated geo/hint reveal). No admin manual, no real$ v1. Per-attempt spends tracked; master = sum earnings - spends (reconcile on sync). *No "coins for rating"*, no animated/voiced, no spend "or for rating". (See 08,01,03,04,06, AN04/coins-economy + review-coins, SYNTHESIS, FINAL §2.)
- Constructor: Form+ordered list + completion picker (physical vs answer + multiline) + *supporting toggles/fields* (gift/hint/video/terminal/narrative); explicit Publish → snapshot + bundle; "save + switch to real player as test user" acceptable (no high-fid required). Templates discussed in ANALYZE-07 variants as *authoring aids* (Physical Observation, Answer/Knowledge, Narrative Bridge/Continue, Gift, Terminal) – *not* client 4 templates. Supports rich media but no "comic-like" or per-role assignment mandated. No navigator config.
- Offline/Progress: Bundle = full GameStep seq + rich content + images + short videos + geo + acceptable list (plain). Physical = confirm (no list). Hints = local spend + reveal. Sync = upload StepCompletions (submitted/confirmed + local is_correct + coins_spent + ts); server *records only* (no re-val per lock); coin recon; mutable attempt state + "last-write or union" for multi-dev (AN05 exposes as racy/fatal per 06 risks); version-bound. *No comic/navigator specifics in bundle.* (03, AN01/offline-model + review-offline, AN05/progress + sync-recording, FINAL.)
- Feedback/Review: Thin Review (player+quest+attempt; grade 1-5? + optional text; post-completion). No "Оставить отзыв" from *any page menu*, no error/bug report flow mid-quest, no context from arbitrary step. (01,06.)
- Marketplace/Catalog: Thin discovery/catalog for viewing/purchase (price, coupon, grant → AccessGrant). Constructor publishes → makes downloadable/purchasable in catalog. Not elevated to "marketplace" as peer component to "quest" + "constructor". No explicit 3-way structure.
- Other: Linear sequence (aligns "sequence of web pages"). Smartphone/PWA (aligns "via smartphone"). No comic style, no navigator button, no wrong-popup hints, no animated/voiced, no coins-rating link, no any-page feedback in any prior/locked/ANALYZE (grep confirmed zero hits for "navigator|comic|Оставить|navigator button|comic-like|coins.*rating|rating.*coin|wrong answer popup|4 templates|First screen|Task no answer" etc. outside generic "review"/"continue"/"template" in constructor variants analysis).

**Harsh Exposure of Flaws in Current Blueprint vs New Req:**
- **Violates client structure:** Blueprint treats "catalog" as supporting to commerce/play (02/00/06); "quest" and "constructor" central. No "marketplace" as first-class peer; some slices (e.g. Progress/Sync AN05, Offline 03) ignore discovery surface entirely. Client makes it explicit 3-component (marketplace/quest/constructor) – mismatch.
- **4 templates vs richer supporting:** Client prescribes *4 templates* as the UI/page skins (First/Task-no/Task-with/Continue). Blueprint (post-AN03/07 + synthesis + FINAL §1/3) deliberately chose *richer composable supporting* (gift/media/terminal/hint/narrative on top of 2 modes) + content as differentiator over "14 Page_type" bloat or hard taxonomy. "Continue" maps to narrative_advance; others don't force 4-hard. Constructor variants invented "templates" as authoring *aids*, not client UI mandate. Violates if client means hard limit (or even primary 4). Our "supporting" richer is good for maintainability (one GameStep shape) but ignores client "4 templates".
- **Physical "move+optional action" vs uniform confirm:** Client example "statue hands" *is in our docs* (04:14, 03:89, 00:12) but locked (08:21-24) "Physical / 'no answer required' steps have no acceptable-answer list. Completion is the player's explicit confirmation action in the app after they have performed the real-world task. 'No difference' between different physical steps — all use the same simple confirmation mechanism." + "we do not need subtypes". Our physical is "confirm" (player honesty); client emphasizes "move + *sometimes perform action*" (may need distinct UX or flag for "action performed" vs pure "found it"). ANALYZE-03/review-gamestep + FINAL attack "trivial confirm reduces the magic" but resolved via *rich content only*, not model change. Mismatch: our uniform may not capture client's "optional action" intent without UI/content support.
- **Navigator button (new, ignored):** Zero in all priors/locked/ANALYZE (grep). Location tasks (physical) get geo display + *optional coin-gated hint reveal* (03/04/01). No "navigator button" (perhaps in-app nav to geo, or "show directions" as optional hint). Client makes it explicit feature for location tasks. Violates; our geo is "display only" (08/03).
- **Comic-like image per page (new, ignored):** Current: "primary image(s), hint_image (coin-gated), video_ref" + rich text (01/03/04). No "comic-like" style mandate, no per-page + roles (task/character/hint/atmosphere). Bundle/Offline silent on image roles beyond hint/primary. Constructor media handling generic. Size risk for 5MB (comics heavier than photos?). Mismatch.
- **Coins: bonuses animated/voiced + spend on hints *or for rating* (new, partial mismatch):** Locked/AN04/08/01/03: earn gifts/completions (snapshot-frozen); spend *hints only*. No animated/voiced (would require assets/events in step/UI). No "for rating" (rating is thin post Review; no coin link or spend-to-rate). "Coins for tasks" aligns partially (gifts + completions), but client ties bonuses directly to tasks + animation/voice. Review-coins + FINAL §2 note "lightweight" but our events/facts model can extend; current locked does not. Mismatch on rating + animation.
- **Hints via wrong answer popup (new detail, ignored):** Current: per-step coin spend (any time? in step UI) reveals geo/hint content (03/04/01). No "via wrong answer popup" (specific trigger on answer tasks after fail). Affects UX (popup flow, wrong-count integration, when coins exchangeable). AN03/05/coins note wrong-count but not hint trigger. Mismatch.
- **Feedback: any-page "Оставить отзыв" for errors + post rate/comment (broader than current):** Current: thin Review post-completion only (01/06). No global menu "Оставить отзыв" from *any page*, no mid-quest error reporting with step context, no separation of bug-report vs rating. 06 requires "Post-completion review". Mismatch on "from menu any page" + error reporting.
- **Progress/Sync (ANALYZE-05 + sync-recording) flaws vs req:** Current (progress variants): mutable QuestAttempt + StepCompletions + "last-write or union" for multi-dev (explicitly exposed as racy in AN05 §2-4: concurrent progress/coins, reset races, version drift, coin recon failures; violates 06 "sync logic has races" risk). No template-specific (4 templates) event logging, no navigator state in completions/sync, no hint-popup flows, no feedback events mid-sequence. Bundle/sync docs (03) focus on confirm/answer + coins_spent; silent on comics/nav/animation triggers. Client "sequence of web pages" + smartphone implies more state (page transitions, navigator usage) to sync. Mismatch + high risk (AN05 calls current "fatal").
- **Offline bundle (03 + AN01 + review-offline + FINAL):** Must now ship *comic images* (per-page, multiple roles), *navigator data* (for location steps; geo + optional button config?), animation/voice assets? Current bundle: "Full GameStep sequence + all rich content (text..., images, short videos..., geo...)". Silent on roles or navigator. ~5MB may be impacted. Mismatch.
- **Constructor (04 + AN07 + review-constructor + FINAL §3):** Supports 2 types + richer supporting (not hard 4 templates). No UI for "comic assignment" (task/character/hint/atmosphere), navigator config (optional per location), template picker enforcing 4, feedback config?, animated/voiced flags, wrong-popup hint wiring. "Save+test" preview acceptable but low fidelity for new graphics/nav flows. "Hard to publish bad" amplified by new visuals (missing comic = dead page). Mismatch (our recs in AN07 variants strengthen gates but not for these client visuals).
- **Marketplace integration (02/00/06/ catalog mentions):** Thin; publish makes "downloadable/purchasable in catalog". Client elevates "marketplace" as peer to quest/constructor. No deep integration details (e.g. constructor publish → marketplace listing auto, with preview comics?).
- **Overall blueprint flaws:** Locked decisions (08) + analyses pre-date or ignore this client req (no incorporation; "new" per task). Rich supporting + uniform physical + thin review + hints-as-spend + catalog-thin prioritize KISS/maintainability vs old 14-types/1163-WFs (good), but *directly conflict with client specifics*. "Content carries atmosphere" (FINAL) helps comics but not mandated style/roles. AN05 Progress exposes races that client "web pages sequence" + any-page feedback + navigator would exacerbate (more mutable state). Ignoring client = blueprint invalid for final SYNTH. Harsh: current is "internally consistent but client-misaligned" on UX/gameplay details that are core to "Описание сайта".

**Rebuild: Resolutions + Mini-Cycle per Mismatch (Deconstruct Edges → Expose → Rebuild Better → Self-Crit):**

1. **Mismatch: 4 templates (hard) vs richer supporting + 2 modes.**
   - Mini deconstruct: Edges – hard 4 means no standalone gift/media/terminal? Or "Continue" can host gift? "First screen" = start only or reusable? Configurable per quest or global? Affects constructor validation + player render (must dispatch exactly 4 UIs?).
   - Expose: Blueprint's richer (gift on any, narrative_advance as Continue, terminal separate) better for expressiveness (old 14 were variants of supporting); client 4 may be "UI templates" not data kinds (map to our modes + supporting). Violates if prescriptive.
   - Rebuild: Map explicitly: First screen → supporting.is_start + narrative or physical/answer with special content; Task no answer → mode=physical (+ optional action flag?); Task with answer → mode=answer; Continue → supporting.narrative_advance (pure or with gift). Allow richer supporting *on* the 4 (e.g. gift+navigator on Task-no). Constructor: template picker (4 primary, prefill mode+supporting); richer flags still available. Player: 4 base renderers + supporting composables. This honors client "4" as primary while keeping our maintainability (one GameStep + small supporting).
   - Self-crit: Still risks "template bloat" if 4 become first-class in data; better as UI concern only (content + mode + flags drive render). If client means *only* 4 allowed, must cut gift-as-standalone (revisit locked?).
   - Chosen: UI-primary 4 + richer supporting allowed (aligns "content first" + client sequence of templated pages).

2. **Mismatch: Physical = move + optional action (statue) vs uniform confirm "no difference".**
   - Mini deconstruct: If action "optional", does confirm UI differ ( "I touched the hands" vs "I found it")? Digital confirmation of physical action impossible without proof (cut per 06/08); honesty still trusted. Affects UX copy per step (rich content + button_text).
   - Expose: Locked "no subtypes" (08) + "uniform mechanism" prevents per-action UI; our resolution was rich content (exactly the statue example in sources). Client may want more (e.g. "action performed" note mandatory?).
   - Rebuild: Keep uniform confirm mechanism (locked). Add optional `supporting.physical_action?: { description: string; confirm_prompt?: string }` (or just rely on rich main_text/place_text/button_text per step – cheaper). Navigator/geo can tie to "location for the action". No digital proof. Constructor: in physical template, optional "action" subfields (pre-filled from content). Player: confirm button text from content or action.
   - Self-crit: Adding flag risks creeping subtypes (violates "no difference"); pure content sufficient and KISS (FINAL §1). But client example emphasizes "perform action" – content + per-step copy is the "rebuild better".
   - Chosen: Content + optional action metadata in supporting (no UI subtype; uniform confirm).

3. **Mismatch: Navigator button for location tasks (optional hint).**
   - Mini deconstruct: Optional per location/physical step? What data (lat/lng + label + "open maps" intent?)? Affects bundle (must ship nav config + perhaps map tiles? – size). UX: button always visible or after hint? Offline: works without net (static data or device maps).
   - Expose: Current geo = "display only" for map pin + hint reveal (03/01/08). No button/nav feature. AN01/05/03 silent. Violates client.
   - Rebuild: Add to GameStep (for physical/location steps): optional `navigator?: { lat: number; lng: number; label?: string; hint_only?: boolean }` (or reuse geo + new flag "has_navigator_button"). Supporting or on physical completion. Constructor: geo picker + "enable navigator button (optional hint)" toggle. Bundle: include in snapshot for steps that have it. Player: on Task-no/ physical template, show "Navigator" button (opens device maps or in-app if offline data). Hint spend can gate the button reveal if "hint_only".
   - Self-crit: Adds data to bundle (minor size); "optional hint" blurs with existing hint system (risk double-hint UX). Pure geo + content may suffice, but client explicit "navigator button".
   - Chosen: Optional navigator data on steps (supporting or physical-specific); integrate with hint if flagged.

4. **Mismatch: Comic-like image per page (task/character/hint/atmosphere roles).**
   - Mini deconstruct: Per-step or per-template? 1 image or 4 roles? Style enforcement (client or just metadata)? Bundle size (comics vs photos; 5MB risk). Offline cache all. Reusable? Assignment in constructor.
   - Expose: Current media generic (primary/hint_image/video); no roles or "comic-like". Bundle/03 mentions "images" generically. Constructor media generic. Violates; "atmosphere" aligns our "rich content primary" but not style/roles.
   - Rebuild: In GameStep rich content/media: `images: { primary?: MediaRef (task), character?: MediaRef, hint?: MediaRef, atmosphere?: MediaRef }` (comic style by convention or author responsibility; or flag `style: 'comic'`). Hint role = the coin-gated one. Constructor: media upload zones *per role* (thumbnails, assign to template). Bundle: all included (packer resolves). Player: render per template/role (Task-no shows primary + atmosphere; hint spend swaps/reveals hint image; character for dialogue?).
   - Self-crit: 4 images/step * 25 steps = bloat risk for 5MB (expose: may exceed; need compression/optional). Roles may over-spec (some steps don't need all); start with primary + hint + optional others. Aligns "per page".
   - Chosen: Role-based images in media (primary/task + hint + optional character/atmosphere); style author-driven or global comic theme.

5. **Mismatch: Coins bonuses (for tasks, animated/voiced) + spend on hints or for rating.**
   - Mini deconstruct: "For tasks" = on every task complete (physical/answer) or gifts only? Animated/voiced: client anim + audio asset (in bundle? per-step or global sounds?). "For rating": spend coins to unlock/submit rating? Derive player "rating score" from coin total? Per-quest or global? Affects Coins model (events now include 'RATING_SPEND' or 'RATING_BOOST'?); UI (post-complete spend flow?).
   - Expose: Locked "hints only" spend + "gifts/completions" earn (08/01/03/AN04). No animation/voice (would be new content type). No rating-coin link (Review thin, no economy). AN04/ review-coins + FINAL recommend facts for robustness – extensible but current locked does not include.
   - Rebuild: Keep earn from tasks (map "bonuses for tasks" to gift on Task templates + completion bonus on terminal). Add to CoinFact kinds: 'TASK_BONUS', 'RATING_SPEND' (or 'RATING_BOOST'). Spend "for rating": optional post-complete flow (if coins >= cost, spend + submit higher-grade Review or unlock rating UI). Animated/voiced: add `supporting.bonus_animation?: { asset_ref, voice_ref? }` (in snapshot; client plays on award). Update Coins invariants + sync payload (if new spend type). Constructor: flag "awards animated/voiced bonus" + asset assign on gift/task steps.
   - Self-crit: "For rating" changes economy (from "lightweight hints" to gamified rating); risks abuse (farm coins to rate high). Animation/voice adds bundle assets (size). If "rating from coins" = derived stat (total coins → visible rating), simpler no new spend. But client says "spend on hints or for rating".
   - Chosen: Extend facts for new spend/earn kinds + animation assets; rating spend as post-quest optional (per-quest or global configurable); keep core "hints primary".

6. **Mismatch: Hints via wrong answer popup (exchange coins).**
   - Mini deconstruct: Trigger only after wrong submit on answer task? Popup shows cost + exchange? Affects answer flow (submit → if wrong: popup with hint option + coins deduct + reveal (answer hint or geo?)). Per-step cost still? Wrong count integration.
   - Expose: Current per-step spend (anytime in step UI, not popup-specific). AN03/05 note wrong-count but not trigger. Violates specific "via wrong answer popup".
   - Rebuild: For answer-mode steps (Task with answer template): on wrong submit → immediate popup "Wrong. Spend X coins for hint?" (uses step's hint.cost or default). On exchange: deduct, reveal (hint_image/content or answer hint text). Still per-step supporting.hint. Log in StepCompletion (wrong + hint spend in one). Constructor: hint fields apply to answer tasks primarily.
   - Self-crit: Popup UX may feel interruptive (vs dedicated hint button); "exchange" = spend. Still fits coin per-step. If only on wrong, misses "proactive hint buy" – allow both?
   - Chosen: Popup as *primary trigger for answer tasks after fail* + keep/allow per-step hint affordance.

7. **Mismatch: Feedback "Оставить отзыв" from menu any page (report errors) + rate/comment after completion.**
   - Mini deconstruct: Global menu (bottom nav? hamburger?) on *every* web page/step; "Оставить отзыв" opens form (pre-filled quest/step/attempt context for bug report). Separate post-complete (on terminal/Continue or quest end): rate (stars) + comment (Review). Bug reports = new entity (FeedbackReport: player, quest, step_pos?, type='error'|'suggestion', text, created_at, synced). Moderation? Visible to admins in constructor stats?
   - Expose: Current only thin post Review (01/06). No any-page, no error reports, no menu. Violates "from menu any page" + "report errors".
   - Rebuild: Add FeedbackReport aggregate (thin, append-only, synced like StepCompletion). UI: global menu component (available in all player pages/templates) with "Оставить отзыв" → form (context auto: current step from attempt state). Post-completion: existing Review flow + "rate/comment". Constructor/quest stats: list reports per quest (read-only). Offline: queue locally, sync on reconnect (idempotent). Ties to Progress/Sync (new event type).
   - Self-crit: Adds entity/sync surface (AN05 already racy; this adds more). "Any page" good for bugs but menu must not clutter smartphone UX. Separate from Review (bug vs rating). Good for quality (authors see errors per step).
   - Chosen: FeedbackReport (bug/error from any-page menu + context) + keep/enhance Review (post rate/comment).

8. **Mismatch: Marketplace as explicit structure component + integration with constructor.**
   - Mini deconstruct: Marketplace = top section for discovery (list quests with comics/previews from constructor), purchase/grants, perhaps player collection. Constructor publish → marketplace visible (with status). Quest player launched from marketplace.
   - Expose: Current "catalog" thin (02/06); publish makes purchasable but no deep "marketplace/quest/constructor" triad or auto-listing with graphics.
   - Rebuild: Elevate "Marketplace" bounded context (or module): catalog view (quests from published Quest snapshots, show comic primary + summary); purchase → grant; links to quest player. Constructor publish flow: "Publish & list in marketplace" (sets visible). Grants/Access in marketplace surface.
   - Self-crit: May be mostly naming/UX (catalog *is* marketplace); adds no new data if we map. But client explicit 3 parts – must reflect in high-level docs/arch (01, vision, pages).
   - Chosen: Document marketplace = catalog + grants discovery; explicit integration (publish surfaces in marketplace with constructor-provided comics).

**Mini-cycles applied to all above (and cross like animated in coins, etc.); resolutions chosen for fidelity to client while preserving KISS/maintainability from priors (one GameStep, facts for coins/sync, snapshot freeze, rich content primary).**

**Synthesize: Updated Invariants (incorporating client or noting violations):**
- Quests = linear sequence of templated web pages/steps (4 primary templates mapped to GameStep modes + supporting; richer supporting allowed for gifts/hints/etc.).
- Exactly 2 task types (physical: move + optional action via content/optional metadata + uniform confirm; answer: enter + match). Physical honesty trusted (no proof v1); "no difference" in *mechanism* only (content + navigator + action metadata differentiate).
- 4 templates primary for player UI/render (First/ Task-no-answer / Task-with-answer / Continue); constructor supports picker + assignment of comics/nav per.
- Graphic: comic-like images assigned per step/page with roles (task/character/hint/atmosphere); included in offline bundles.
- Navigator: optional supporting data + button for location/physical steps (can be hint-gated); geo display + nav action.
- Coins: earned on tasks (via gifts on steps + completion bonuses, snapshot-frozen); *animated + voiced* on award (assets in content). Spend: hints (primary, via wrong-popup on answer tasks or per-step) *or for rating* (post-quest optional spend to enable/boost Review). Extend CoinFact kinds + events. Per-attempt spend tracking + global projection.
- Hints: coin exchange; primary trigger wrong-answer popup on answer tasks (reveals hint content/image/geo/nav?); per-step cost from supporting.
- Feedback: any-page global menu "Оставить отзыв" for error/bug reports (FeedbackReport with step context, synced); + post-completion rate/comment (Review). Both thin + author-visible.
- Marketplace: explicit top component (catalog of constructor-published quests with comic previews + grants/purchase); integrates publish → list.
- All versioned in snapshots at publish (comics, navigator config, animation refs, hint costs, template prefs if any, gift amounts); old attempts frozen.
- Offline/PWA/smartphone: full bundle includes comics (roles), navigator data, animation/voice refs, template metadata; local validation/play for tasks/hints/coins (incl. popup flows); sync records StepCompletions + new FeedbackReports + coin facts (idempotent).
- Progress/Sync: append-only facts/events (not just mutable completions) for *all* (step advances per template, navigator uses, hint-popup exchanges, any-page feedbacks, coin awards/spends incl. rating, animated triggers). Resolves races from AN05.
- Constructor: supports exact 4 templates + comic role assignment + navigator config + animation flags + hint popup wiring + feedback? preview. Publish produces bundle shape with all new fields.
- Linear, Russian v1, internal constructor, AccessGrant before play, client local for snapshot, etc. (preserved where not conflicting).
- New: "Hard to publish bad" now includes missing comics, bad navigator, unassigned template images, etc. (gates in constructor).

**Impacts on All Slices (from rebuild):**
- **GameStep (01/04/AN03/FINAL §1/review-gamestep):** Must support 2 types + 4 templates *as primary UI* (add template?: 'first'|'task_no'|'task_with'|'continue' or derive from mode+supporting.narrative_advance+is_start; richer supporting still composable for gifts/hints/nav/comics/animation). Physical: optional action metadata + navigator. Media: role-based comics. Snapshot freezes all (incl. new). Cross: richer but still one shape (KISS win vs 14 old).
- **Coins (08/01/03/AN04/review-coins/FINAL §2):** Extend for task bonuses + animated/voiced (supporting + assets) + rating spend (new fact kind + post-quest flow). Projection includes new. Snapshot-frozen costs/amounts. "For tasks" = explicit on Task templates.
- **Constructor (04/AN07/review-constructor/FINAL §3):** UI for 4-template picker (prefills mode/supporting), per-role comic uploads (task/character/hint/atmosphere), navigator toggle + geo picker on physical/location, animation/voice asset assign on bonuses, hint "wrong popup" wiring (default for answer). Strengthened gates (must have primary comic per task template? navigator data valid?). Publish snapshots expanded shape. "Test player" must exercise new flows (popup, nav button, animated award, any-page feedback).
- **Offline (03/AN01/review-offline/FINAL):** Bundle *must* pack comics (roles), navigator data/config, animation/voice refs (in addition to prior images/video/geo). Local play: render per template, handle navigator button (static), play anim/voice on bonus, trigger hint popup on wrong + spend. ~5MB may need revisit (compression). Size est in constructor.
- **Progress/Sync (AN05/progress-attempt-sync-variants + sync-recording/FINAL/03):** *Must* move from mutable+last-write/union to event-sourced facts (append-only for step complete per template, navigator interaction, hint popup exchange, FeedbackReport creation mid-sequence, coin award/spend incl rating, anim trigger). Sync payload expands (new event types + context). Idempotency keys critical (per AN05 races). Multi-dev merge by facts (union commutative). Corrections for coins/feedback. Version-bound. "Sequence of web pages" state (current template/page) in events. AN05 rec strengthened to "non-negotiable" by new client flows.
- **Feedback/Review (01/06):** Split/enhance: FeedbackReport (any-page, error-focused, step context, menu-driven) + Review (post rate/comment). Both synced like completions. Constructor visibility.
- **Marketplace/Catalog/Commerce (02/00/06):** Elevate as peer; publish from constructor surfaces quests (with comic primary + template previews) in marketplace. Grants/purchase flows through it. Discovery uses new graphics.
- **Domain (01) + Vision (00) + v1 cuts (06) + Assumptions (07):** Update ER (add FeedbackReport, navigator on GameStep, role images, CoinFact extensions). Invariants expanded (above). Risks: bundle size, new races in sync from popup/nav/feedback events (must use facts), frozen bad comics/graphics permanent.
- **Cross all:** Bundle size (5MB) risk higher (comics + audio); constructor velocity (more fields per step); analytics per-version now includes template usage, navigator clicks, popup hints, feedback reports; migration: map old Page_type to templates + comics (primary from old images?); nonfunc: PWA cache for new media types.

**Point-by-Point Table (Client Point | Current Blueprint | Mismatch? | Resolution | Why Chosen | Cross Impacts | Risks):**

| Client Point | Current Blueprint (locked + ANALYZE + FINAL/SYNTH) | Mismatch? | Resolution | Why Chosen | Cross Impacts | Risks |
|--------------|-----------------------------------------------------|-----------|------------|--------------|---------------|-------|
| Structure: marketplace/quest/constructor (3 components) | Catalog (thin discovery/purchase in 02/06/00); quest player + constructor central. No "marketplace" as peer top-level. | Yes (naming + elevation; some slices ignore catalog). | Elevate Marketplace = catalog + grants surface; explicit publish → list in marketplace (with constructor comics). | Client explicit; aligns existing catalog but makes 3-way structure clear for UX/arch. | 02/01/06/00 update; constructor publish flow; player launch from market. | Mostly doc/UX; low if catalog already works. |
| Gameplay via smartphone; sequence of web pages | PWA/mobile implied (03/00/06); linear GameStep sequence (01/04). "Web pages" not literal. | Partial (smartphone aligns; "web pages" may imply browser nav/transitions). | Treat as PWA pages per template; support back/forward/progress in player. | Client "sequence of web pages" on smartphone = paging UI per step. | Player render (templates); sync events for page advances. | Smartphone PWA already targeted; literal web may conflict offline. |
| 2 task types: no answer (move+optional action e.g. statue hands); with answer (enter correct e.g. knowledge/plaque/count/rebus) | Exact match on 2 types + examples (statue in 04/03/00); physical = confirm after real-world (08/01/03). | Partial (examples match; "optional action" + "move" emphasis not in uniform model). | Keep uniform confirm mechanism (locked); add optional action metadata + rich content for "perform". Map to Task-no / Task-with templates. | Preserves locked "no difference"/honesty (08); uses content for differentiation (FINAL §1); captures client phrasing. | GameStep physical (01/04); player UI copy per template; constructor physical editor. | "Action" may still feel trivial without proof (self-crit from 07/03). |
| 4 templates: First screen, Task no answer, Task with answer, Continue | 2 modes + richer supporting (gift/media/terminal/narrative_advance/ is_start) (01/04/AN03/07/FINAL). "Continue" maps to narrative_advance. No hard 4. Authoring templates in AN07 variants (not client UI). | Yes (richer vs hard 4; no "First screen"/"Task no/with" as primary). | Map 4 as primary UI/templates (First=is_start/narrative; Task-no=physical; Task-with=answer; Continue=narrative_advance); allow richer supporting on them. Constructor template picker. | Client prescriptive for "sequence of ... pages"; our richer enables gifts etc. without bloat (one shape); maps cleanly. | GameStep (add template field or derive); Constructor (AN07 recs); Player render dispatch; bundle. | If "hard 4 only", cuts expressiveness (gift standalone); risk of template taxonomy creep. |
| Graphic: comic-like image *per page* for task/character/hint/atmosphere | Primary image(s) + hint_image + video (generic) (01/03/04). Rich content for atmosphere. No comic style, no per-role, no "per page" mandate. | Yes (generic media vs specific comic roles + style + per-page). | Role-based images in GameStep media: {task/primary, character, hint, atmosphere}; comic by author convention or flag. Per-step assignment. Bundle includes all. | Directly satisfies "comic-like per page" + roles; "atmosphere" aligns our content primary (FINAL); offline requires. | Offline bundle (03/AN01); Constructor media zones (AN07); Player per-template render; size (~5MB). | Bundle bloat (4 images/step); some steps unused roles; style enforcement weak (author). |
| Navigator button for location tasks (optional hint) | Geo {lat,lng} display-only + coin-gated hint reveal for physical/location (08/01/03/04). No "button" or "navigator". | Yes (no feature at all). | Optional `navigator` supporting or on physical: {lat,lng,label, hint_only?}. Constructor geo+toggle. Player button on Task-no (device maps or in-app). Hint can gate. | Client explicit for location (physical) tasks; optional fits "hint"; reuses geo. | GameStep (01/04); Offline (nav data in bundle); Sync (navigator events?); Constructor; UX for physical. | Size (map data?); UX overlap with existing hint/geo; optional = more conditionals. |
| Bonuses (coins for tasks, animated/voiced, spend on hints or for rating) | Earn gifts (per GameStep snapshot) + completions (08/01/03/AN04). Spend *hints only*. No animated/voiced, no rating link/spend. "Coins for tasks" partial (gifts on steps). | Yes (spend options, animation/voice, rating tie-in new; bonuses align partially). | Extend CoinFact for 'TASK_BONUS' etc.; supporting.bonus_animation {asset, voice?}; rating spend (post-quest, new fact kind or boost Review). Award on task completes. | Client "or for rating" + "animated/voiced" + "for tasks"; facts model (AN04/FINAL) extensible; keeps lightweight core. | Coins (01/03/08/AN04); GameStep supporting + assets; Sync/Progress (new facts); Constructor (asset assign + flag); Post-complete flow. | Economy change (rating spend may feel pay-to-rate); bundle audio size; abuse on rating. |
| Hints (via wrong answer popup, exchange coins) | Per-step coin spend (anytime) reveals geo + hint content (03/01/04). No popup trigger, no "wrong answer" specific. | Yes (trigger + "via wrong popup" + "exchange" detail absent). | For answer/Task-with: on wrong submit → popup "Spend X to exchange for hint?" (uses supporting.hint.cost). Deduct + reveal. Still per-step. | Matches client exact trigger for answer tasks; "exchange" = spend; integrates wrong count (AN03). | GameStep answer + hint (01/04); Player answer flow + popup (templates); Coins spend; Sync (popup spend fact); Constructor. | Popup interruptive UX; misses proactive hints (allow button too?); only answer tasks. |
| Feedback (report errors from menu any page "Оставить отзыв"; rate/comment after completion) | Thin Review (post-completion grade+text only) (01/06). No any-page menu, no mid-quest error reports, no "Оставить отзыв". | Yes (scope, timing, menu, error vs rating separation absent). | FeedbackReport entity (any-page menu-driven, error/bug type, auto step/quest context, synced). Separate/enhance Review for post rate/comment. Global menu in all player pages. | Client "from menu any page" + "report errors" + post rate; thin entities fit (like Review); improves author feedback loop. | Domain (01 add entity); Progress/Sync (AN05: new event/fact type + queue); Player (menu in all 4 templates); Constructor (reports list per quest); Offline (local queue). | Adds sync surface (races per AN05); menu clutter on smartphone; moderation for reports. |
| (Cross) Marketplace integrates with constructor (publish → catalog with graphics) | Publish makes "downloadable/purchasable in catalog" (04/02/06). Graphics generic. | Partial (integration exists at high level; no comics/nav in listing, no "marketplace" framing). | Marketplace surface lists published (from constructor) with comic primary + template summary; publish flow "list in marketplace". | Client 3-component + "how integrates"; existing catalog is base. | Commerce (02); Constructor publish (04/AN07); Discovery/catalog UI (new graphics). | Low data change; UX polish. |

**Additional cross-mismatches (table condensed):** Progress/Sync races (AN05) exacerbated by new flows (popup, nav, any-page feedback, animated awards, web-page sequence state) – resolution: enforce event-sourced facts (append-only AttemptEvent/CoinFact/FeedbackFact) over mutable (per AN05 rec, hardened). Offline bundle must explicitly document/include new assets (comics roles, nav, anim/voice) – resolution: expand 03 bundle spec + packer. Constructor "hard to publish bad" now includes visual/UX badness (missing comic, bad nav pin) – resolution: pre-publish checklist + mini-previews per AN07 rec + client req visuals.

**Self-Critique of This Analysis (as mandated):**
- Strengths: Full cycle (deconstruct + compare + harsh expose + rebuild + mini-cycles + synthesize + table); max cross-ref to *every* listed prior/locked/ANALYZE (reads/greps of 08/01/03/04/06/07 + FINAL + SYNTHESIS + all reviews + variants + discovery); grounded (no invention; statue example shared with client); table exhaustive; status/path concrete; absolute paths; extreme skepticism (called out even "good" resolutions like richer supporting as potential violation); incorporated client or highlighted violations.
- Weaknesses: New req from query paraphrase (no raw "Описание сайта" file found via grep/list; treated as authoritative per task). No real 556-row export to validate 4-template mapping or comic usage (repeated gap in priors/AN03/07/09). "Marketplace" may be just re-naming (low risk). Animated/voiced + rating spend may be "delight" details not core domain (but client lists them). Bundle size 5MB (08) now higher risk – not re-estimated quantitatively here. Progress AN05 already in flight; this hardens its rec but does not re-run full variants.
- Process fidelity: Used list_dir/grep/read_file (broad-to-narrow, multiple strategies: term searches for client keywords (zero hits confirm mismatch), full reads of locked + FINAL + SYNTH + reviews + partial variants); parallel calls; no broadening; todo not used (single-task focus); report created only at end via mandated write (necessary per task). No code edits (pre-impl). Citations not applicable (no web/X; all internal).
- Overall: Blueprint was "internally robust vs old Bubble" (KISS one-shape, facts, snapshots, content-first) but *client-misaligned* on gameplay/UX specifics that are the product (templates, comics, navigator, hint UX, feedback any-page, coins delight/rating, 3-structure). Resolutions preserve strengths while incorporating. Critical for "before final SYNTH".

**Status + Path (at end, as mandated):**
**Status:** COMPLETE. All prior ANALYZE + locked + new client req deconstructed/exposed/reconciled. Mismatches real and multiple (structure, templates vs richer, physical details, navigator, comics, coins extensions, hint trigger, feedback breadth, bundle/sync impacts). Resolutions proposed with mini-cycles; updated invariants + slice impacts synthesized. Report written to absolute path.

**Path (concrete, actionable, no scope creep; feeds SYNTH + updates):**
1. Stakeholder review this report + query client req source if raw "Описание сайта" PDF/export exists (discovery/raw/pdf_images? or external).
2. Lock key resolutions in business/08_DECISIONS_LOG.md (new section "2026-06 — Client Requirements Incorporation (marketplace structure, 4 templates as primary, physical action+navigator, comic roles, coins animated/rating spend, hint popup, any-page Оставить отзыв/FeedbackReport, publish→marketplace)").
3. Propagate: update business/01 (GameStep media roles + navigator + FeedbackReport + template field/derivation + CoinFact extensions + invariants); business/03 (bundle spec for comics/nav/anim + facts in sync); business/04 (constructor capabilities for 4 templates + roles + navigator config + assets + gates); business/06 (add any-page feedback, navigator, comics, marketplace as core); business/00/02/07 (vision/assumptions cross).
4. Update FINAL-BEST-PRACTICE-BLUEPRINT.md + SYNTHESIS-NOTE (add section for this analysis; revise GameStep/Coins/Constructor/Offline/Progress slices per rebuild); analysis/README.md (record as parallel task complete).
5. For ongoing ANALYZE-* (esp. Progress/Sync AN05 + sync-recording): re-attack with new client flows (more events: navigator use, popup exchange, FeedbackReport mid-sequence, template advances, anim triggers); enforce facts model.
6. When real quest data (page_constructor export + 2-3 full quests walk, per repeated rec in AN03/07/09/this): validate 4-template mapping, typical comic counts/roles, navigator usage, "action" vs pure move frequency, wrong-popup as hint UX.
7. Pre-impl: define shared types (expanded GameStep with roles/nav/template/anim, FeedbackReport, CoinFactKind extensions); golden bundle fixtures including new assets; constructor validator for "every Task template has primary comic".
8. Risks to monitor: bundle >5MB (measure with real comics); sync races from new mid-quest events (use facts + keys); constructor velocity (more per-step fields – mitigate with templates/presets); client "hard 4" vs our richer (measure if authors need standalone gift/media outside templates).
9. Next: full SYNTH-001 incorporating this (before any code); then domain model freeze + data model (docs/02 etc.).

**Absolute paths referenced (key):**
- Report: `/home/nabor/_projects/geohod/quests/business/analysis/client-requirements-mismatch-analysis.md` (this).
- Locked: `/home/nabor/_projects/geohod/quests/business/08_DECISIONS_LOG.md`, `/home/nabor/_projects/geohod/quests/business/01_DOMAIN_MODEL_AND_CONCEPTUAL_SCHEMA.md`, `/home/nabor/_projects/geohod/quests/business/03_OFFLINE_PWA_AND_PROGRESS_MODEL.md`, `/home/nabor/_projects/geohod/quests/business/04_CONTENT_MODEL_AND_QUEST_CONSTRUCTOR.md`, `/home/nabor/_projects/geohod/quests/business/06_V1_REQUIREMENTS_AND_CUT_LIST.md`, `/home/nabor/_projects/geohod/quests/business/07_ASSUMPTIONS_RISKS_AND_OPEN_QUESTIONS.md`.
- Blueprint: `/home/nabor/_projects/geohod/quests/business/analysis/FINAL-BEST-PRACTICE-BLUEPRINT.md`, `/home/nabor/_projects/geohod/quests/business/analysis/SYNTHESIS-NOTE-GAMESTEP-COINS-CONSTRUCTOR.md`.
- ANALYZE + reviews: `/home/nabor/_projects/geohod/quests/business/analysis/gamestep-completion-variants.md`, `review-gamestep.md`, `coins-economy-variants.md`, `review-coins.md`, `constructor-authoring-variants.md`, `review-constructor.md`, `offline-model-variants.md`, `review-offline.md`, `progress-attempt-sync-variants.md`, `sync-recording-variants.md` (and others in analysis/).
- Discovery (for counts/WFs): `/home/nabor/_projects/geohod/quests/discovery/parsed/data_types.json`, `element_definitions.json`, `option_sets.json`, `workflows_all.json`.

This completes the assigned task. All client req points incorporated or mismatches highlighted with resolutions. Ready for blueprint update + SYNTH. 

(End of report.)