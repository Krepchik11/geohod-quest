// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ReviewWire } from "./ReviewWire";
import type { StartPointWire } from "./StartPointWire";

/**
 * Product page payload (§3.1/§12.9): ONLY model data — the published card, the
 * live rating aggregates, the constructor's store description, author display
 * name + how many of their quests are on sale, and snapshot-derived content
 * chips. Visibility matches the catalog: a delisted quest 404s here too.
 */
export type ProductPageWire = { rating_avg: number, rating_count: number, 
/**
 * Public players counter: real distinct completions + marketing `players_bonus`.
 */
players: number, 
/**
 * Author display label from the constructor row; None for legacy/direct
 * publishes that have no constructor lifecycle.
 */
author_name: string | null, 
/**
 * How many of this author's quests are currently on sale.
 */
author_published_count: number, 
/**
 * §11 reviews: newest-written first — the first page; the rest comes from
 * GET /api/quests/{id}/reviews.
 */
reviews: Array<ReviewWire>, 
/**
 * Total ratings that carry text («{M} с отзывом»).
 */
reviews_total: number, 
/**
 * «Место старта» — see [`snapshot_start_point`]; None hides the button.
 */
start_point: StartPointWire | null, quest_id: string, name: string, primary_comic: string | null, template_summary: string, snapshot_version: number, 
/**
 * Frozen snapshot identifier new attempts bind to (e.g. "golden-mystery-fortress-v1").
 */
snapshot_id: string, 
/**
 * Store-card city (e.g. "Нови Сад"); None when the author left it blank.
 */
city: string | null, 
/**
 * Store-card duration label (e.g. "1.5 часа"); None when blank.
 */
duration: string | null, 
/**
 * Price in whole rubles; Some(0) is an explicitly free quest, None is unset.
 */
price: number | null, 
/**
 * Store description from the constructor settings (product page, §3.1).
 */
description: string | null, 
/**
 * Content chips derived from the frozen snapshot at publish time; None for
 * versions published before the chips existed (the UI hides unknown chips).
 */
pages: number | null, tasks: number | null, paid_hints: boolean | null, };
