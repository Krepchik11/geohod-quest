// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.

/**
 * Marketplace catalog row: the stored [`PublishedMeta`] plus the live aggregate
 * rating. The rating is a pure projection of the current published version's
 * finale `quest_rated` facts (never stored), so a freshly published quest reports
 * `rating_count: 0` and the client shows "no ratings yet" instead of a fake score.
 */
export type PublishedQuestWire = { rating_avg: number, rating_count: number, 
/**
 * Public players counter: real distinct completions + the author's marketing
 * `players_bonus`. The raw bonus is never sent on its own (see PublishedMeta).
 */
players: number, 
/**
 * Author attributes from the constructor row (store-page filters); `None` /
 * empty for a legacy/direct publish that has no constructor row.
 */
complexity: string | null, age_target: string | null, tags: Array<string>, quest_id: string, name: string, primary_comic: string | null, template_summary: string, snapshot_version: number, 
/**
 * Frozen snapshot identifier new attempts bind to (e.g. "golden-mystery-fortress-v1").
 */
snapshot_id: string, 
/**
 * Store-card city (e.g. "Нови Сад"); None when the author left it blank.
 */
city: string | null, 
/**
 * Store-card duration label (e.g. "1.5 часа"); None when blank.
 */
duration: string | null, 
/**
 * Price in whole rubles; Some(0) is an explicitly free quest, None is unset.
 */
price: number | null, 
/**
 * Store description from the constructor settings (product page, §3.1).
 */
description: string | null, 
/**
 * Content chips derived from the frozen snapshot at publish time; None for
 * versions published before the chips existed (the UI hides unknown chips).
 */
pages: number | null, tasks: number | null, paid_hints: boolean | null, };
