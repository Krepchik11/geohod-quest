## 1. Prep, critical reads, and TDD skeleton (fail fast, goldens contract first)

- [x] 1.1 Re-run `openspec status --change "backend-facts-sync" --json | cat` and confirm proposal/design/specs done, tasks ready; re-read proposal.md (why + capabilities), design.md (all decisions on enum Fact, natural keys, pure projectors, in-mem store+swap, correction protocol, manifest, rust.md), specs/facts-sync/spec.md (all 9 reqs + scenarios, esp. golden fidelity, idemp on reconnect, overdraft correction, multi-device union, historical re-mat).
- [x] 1.2 Re-read critical sources inside scope: backend/src/main.rs (build_router, AppState, health test pattern with oneshot), backend/src/errors.rs (current AppError + IntoResponse), backend/src/config.rs, backend/Cargo.toml (serde/axum/thiserror/anyhow present, no new deps), frontend/goldens/playthrough-happy-with-gift.json (exact expected_facts array + bal=5), frontend/lib/shared-model.ts (Fact shape, projectBalance sum, projectState fold for completed/revealed), frontend/lib/__tests__/player-replay.test.ts (how goldens drive asserts), agents/rust.md (NEVER .unwrap prod, Result, doc comments on ALL pub, #[cfg(test)] mod, 4-space, clippy -D, Arrange-Act-Assert, meaningful names), archived quest-player-pwa design (simulate sync stub + facts as contract).
- [x] 1.3 In backend/src/ create facts.rs skeleton with `pub mod facts { ... }` (or flat) containing: use serde::{Deserialize, Serialize}; #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)] #[serde(tag = "type", rename_all = "snake_case")] pub enum Fact { PhysicalConfirmed { step_position: i32, submitted_value: Option<String>, local_is_correct: bool, coins_delta: i32, note: Option<String>, device_id: String }, ... (all 7 current + BalanceCorrected { step_position: i32, coins_delta: i32, reason: String, device_id: String, note: Option<String> }, VersionMismatchCorrected { ... }); plus `pub fn coins_delta(&self) -> i32 { match ... }`; empty `pub fn project_balance(facts: &[Fact]) -> i32 { 0 }` etc. Add `#[cfg(test)] mod tests { use super::*; }`. Verify compiles: `cargo check -p geohod-backend 2>&1 | tail -5`.
- [x] 1.4 Create initial RED test in facts.rs tests: embed `const HAPPY_FACTS_JSON: &str = r#"[{ "type": "physical_confirmed", ... exact from golden happy expected_facts + gift at 2 with +5 ... }]"#; ` then `#[test] fn projectors_match_golden_happy() { let facts: Vec<Fact> = serde_json::from_str(HAPPY_FACTS_JSON).expect("test data"); assert_eq!(project_balance(&facts), 5, "RED until impl"); }` + similar for project_state completed/revealed. Run to see RED: `cargo test -p geohod-backend facts::tests::projectors_match_golden_happy -- --nocapture 2>&1 | cat` (must fail on assert or not found).

## 2. Pure projectors + idempotency helpers + golden parity (TDD core, must match client exactly)

- [x] 2.1 Implement project_balance as simple `facts.iter().map(|f| f.coins_delta()).sum()` (pure, no side effects).
- [x] 2.2 Implement project_state mirroring TS exactly: collect completed from physical_confirmed | answer_submitted | attempt_completed (push pos, later dedup+sort), revealed from hint_purchased; return struct `#[derive(Debug, Clone, PartialEq, Serialize)] pub struct ProjectedState { pub completed_steps: Vec<i32>, pub balance: i32, pub revealed_hints: Vec<i32> }` (note: snake for Rust; response can rename if needed later).
- [x] 2.3 Implement project_analytics (for PLAN per-version): struct Analytics { hints_used: usize, wrongs_submitted: usize, navigator_clicks: usize, feedback_count: usize }; fold counting hint_purchased, answer_submitted && !local_is_correct, navigator_used, feedback_reported.
- [x] 2.4 Add `pub fn natural_key(f: &Fact) -> (String, i32, String, Option<String>, i32, Option<String>)` (or equivalent tuple for device, pos, type-as-str, submitted, delta, note) + `pub fn semantically_same(a: &Fact, b: &Fact) -> bool { natural_key(a) == natural_key(b) }`.
- [x] 2.5 Make the RED test green for happy: fill projectors so `cargo test -p geohod-backend facts::tests::projectors_match_golden_happy -- --nocapture` passes (bal==5, state completed [0,1,3], revealed empty). Add second test for extra edges (wrong+ hint+feedback+nav appended to happy base; assert bal>=5, revealed contains 1, analytics counts).
- [x] 2.6 Add unit test for idemp helpers: construct two identical Facts (diff device? no, same natural), assert semantically_same true; one differing note or delta, false. Run: `cargo test -p geohod-backend facts:: -- --quiet`.
- [x] 2.7 Add doc comments to all pub in facts.rs (/// Fact enum ... # Examples ``` ... ``` ; same for fns with # Arguments # Returns # Errors). Verify no missing: `cargo doc -p geohod-backend --no-deps 2>&1 | tail -3` (or grep later).
- [x] 2.8 Full parity gate vs golden data: run the test with extra cases + empty log (bal=0, empty vecs); also `cargo test -p geohod-backend -- --quiet 2>&1 | cat`.

## 3. In-memory store + snapshot manifest (inmem first, swap path explicit)

- [x] 3.1 Create (or extend facts.rs / new store.rs) `pub struct InMemoryFactStore { fact_logs: std::collections::HashMap<String, Vec<Fact>>, attempt_snapshots: std::collections::HashMap<String, String>, snapshot_manifest: std::collections::HashMap<String, SnapshotManifestEntry> }` where `#[derive(Clone, Debug, Serialize, Deserialize)] pub struct SnapshotManifestEntry { pub snapshot_id: String, pub stored_at: u64, pub data: Option<serde_json::Value> }`.
- [x] 3.2 Impl methods on store: `pub fn append_idempotent(&mut self, attempt_id: &str, incoming: Vec<Fact>, snapshot_id: Option<&str>) -> (Vec<Fact>, Vec<Fact>)` (accepted, corrections); inside: entry.or_default(), for each if !semantically_same any existing, push to accepted + log; if snapshot, bind; then call maybe_generate_corrections (stub for now) which may push more to log and return them. Return the two vecs + side-effect the log.
- [x] 3.3 Add `pub fn get_projected(&self, attempt_id: &str) -> Option<(ProjectedState, String /*snap*/, usize /*count*/)>` using the logs + projectors.
- [x] 3.4 Add bind + manifest helpers: on first append or explicit, insert manifest entry (data=None for YAGNI); `get_manifest` etc. Document in code: // Future: trait FactStore { fn append... } ; impl for JsonlFactStore etc. No behavior change.
- [x] 3.5 Unit tests for store (in cfg(test)): create store, append batch, assert accepted len, get_projected bal==5 for happy, second identical batch -> accepted 0, projected unchanged. Run `cargo test -p geohod-backend store -- --nocapture 2>&1 | cat`.
- [x] 3.6 Test manifest: append with snapshot_id, assert get_projected includes the id; re-mat sim: clear? (new store), re-append same facts, projectors still give 5.

## 4. Errors, AppState, router wiring (minimal, test-friendly)

- [x] 4.1 Edit backend/src/errors.rs : extend AppError with `#[error("bad request: {0}")] BadRequest(String), #[error("conflict: {0}")] Conflict(String)` (or VersionMismatch variant); update IntoResponse match to return StatusCode::BAD_REQUEST / CONFLICT with json { "error": msg } for them; keep Internal 500. Add docs.
- [x] 4.2 Edit backend/src/main.rs : add `mod facts; mod store;`; use facts::{Fact, ProjectedState, project_balance}; use store::InMemoryFactStore; in AppState add `store: std::sync::Arc<std::sync::Mutex<InMemoryFactStore>>`, init in main as Arc::new(Mutex::new(InMemoryFactStore {..})); update build_router to accept & use the extended state.
- [x] 4.3 In build_router add the routes: `.route("/api/attempts/:attempt_id/facts", axum::routing::post(append_facts_handler)) .route("/api/attempts/:attempt_id/state", axum::routing::get(get_state_handler))` (after health).
- [x] 4.4 Add thin DTOs near handlers or in facts: `#[derive(serde::Deserialize)] struct AppendRequest { facts: Vec<Fact>, snapshot_id: Option<String> }`; `#[derive(serde::Serialize)] struct SyncResponse { accepted: Vec<Fact>, corrections: Vec<Fact>, projected: ProjectedState, snapshot_id: String, fact_count: usize }`.
- [x] 4.5 Verify router still builds + health test unaffected: `cargo check -p geohod-backend && cargo test -p geohod-backend --test health_returns_ok_and_version -- --quiet 2>&1 | cat` (or the mod test).

## 5. Handlers (append + state; use store + projectors; Result everywhere)

- [x] 5.1 Implement (in main or facts) async fn append_facts_handler( State(state): State<AppState>, Path(attempt_id): Path<String>, Json(req): Json<AppendRequest> ) -> Result<Json<SyncResponse>, AppError> { let mut s = state.store.lock().map_err(|e| AppError::Internal(anyhow::anyhow!("lock: {}", e)))?; let (acc, corr) = s.append_idempotent(&attempt_id, req.facts, req.snapshot_id.as_deref()); let (proj, snap, cnt) = s.get_projected(&attempt_id).unwrap_or_default_proj(); Ok(Json(SyncResponse { accepted: acc, corrections: corr, projected: proj, snapshot_id: snap, fact_count: cnt })) } (handle bad via ? if serde failed upstream).
- [x] 5.2 Implement get_state_handler similarly (no append): lock, get_projected or 404/NotFound via new error variant if wanted, return projected + meta.
- [x] 5.3 Wire any missing use statements, handle empty attempt (create on first append).
- [x] 5.4 Quick manual verify (no full tests yet): `cargo test -p geohod-backend 2>&1 | tail -10` (existing still pass).

## 6. Corrections, manifest binding, and edge behavior (per design + all spec scenarios)

- [x] 6.1 Implement (pure or in store) stub `fn maybe_generate_corrections(prior: &[Fact], just_accepted: &[Fact]) -> Vec<Fact>` (for now return vec![] or one simple case: if any just_accepted hint_purchased and final bal < 0, emit BalanceCorrected +abs, reason "overdraft_on_hint").
- [x] 6.2 Call it inside append_idempotent after the new facts are in the log; include returned corrections both in the returned vec and appended to log (so next get sees them).
- [x] 6.3 Update append handler to surface corrections in response; ensure projectors treat correction variants (their coins_delta affects bal).
- [x] 6.4 Add binding: if snapshot supplied on append and attempt not yet bound, set it + manifest entry (data: None).
- [x] 6.5 Unit + store test for correction path: setup store with low bal facts from "device-b", append -5 hint from "device-a", assert corrections non-empty with positive delta, final projected bal not negative.
- [x] 6.6 Version mismatch test in store/handler: append with different snap -> corrections has mismatch fact, binding unchanged.

## 7. Full integration tests via oneshot (prove every spec scenario + goldens)

- [x] 7.1 In main.rs #[cfg(test)] mod (or facts integration) add use tower::ServiceExt; use axum::{body::Body, http::{Request, StatusCode}}; ... ; build full app with test state (empty store or preloaded).
- [x] 7.2 Test idempotent reconnect: POST happy batch, assert accepted==4, bal==5; POST identical again, accepted==0, bal still 5, fact_count==4. Verify with `cargo test ... -- --nocapture`.
- [x] 7.3 Test golden fidelity end-to-end: POST the exact happy expected_facts JSON deserialized, GET /state, assert body json projected.balance==5 and completed match.
- [x] 7.4 Test multi-device union: POST from device-a some facts, POST overlapping + new from device-b, assert total unique count, no double bal, GET state same from both.
- [x] 7.5 Test overdraft correction + response: setup prior, POST bad spend, assert response.corrections has balance_corrected, projected updated, subsequent GET includes the correction fact.
- [x] 7.6 Test manifest + re-mat: POST with snapshot_id, assert in response; (sim restart by new AppState), re-POST facts or GET, projectors give same state + snap id present.
- [x] 7.7 Test batch mix + errors: POST bad payload (missing device), assert 400 no append; POST mixed dupe+new, assert only new accepted.
- [x] 7.8 Run full integration suite: `cargo test -p geohod-backend 2>&1 | cat` (all new + old green); count tests `cargo test -p geohod-backend -- --list | grep -c 'test '`.

## 8. Rust quality gates, docs, fmt, clippy, no-unwrap audit (strict per rust.md + briefing)

- [x] 8.1 Run `cargo fmt -p geohod-backend -- --check 2>&1 | cat` (must be clean; if not, `cargo fmt` but prefer edit to satisfy).
- [x] 8.2 `cargo clippy -p geohod-backend -- -D warnings 2>&1 | cat` (zero warnings; fix any in new code, esp. needless_return, unwrap_used in non-test).
- [x] 8.3 Audit new prod paths: `grep -n "\.unwrap\|\.expect" backend/src/facts.rs backend/src/main.rs backend/src/errors.rs backend/src/store.rs 2>/dev/null | grep -v '#\[cfg(test)\]\|test ' || echo "No prod unwraps (good)"`.
- [x] 8.4 Full doc + build gate: `cargo doc -p geohod-backend --no-deps 2>&1 | tail -5 && cargo build -p geohod-backend 2>&1 | tail -3` (clean).
- [x] 8.5 Verify all pub have docs + tests in mods: manual grep or `cargo test` coverage implicit.
- [x] 8.6 Player contract note (YAGNI no change): `grep -n "simulate sync\|handleSimSync" frontend/app/quest/QuestPlayerClient.tsx frontend/app/quest/OfflineBanner.tsx || echo "stub untouched (good)"`; add comment in client if helpful but do not wire fetch yet.

## 9. Self-critique + final gates + close (apply full adversarial cycle to proposal/design/specs/tasks + impl readiness)

- [x] 9.1 Re-apply full Cycle to the artifacts + design: Deconstruct (re-check every edge from initial: concurrent multi-device appends under lock, fact without ts for key, version drift mid-attempt, overdraft timing vs gift, long queue order, empty vs terminal, analytics not exposed, manifest data=None, in-mem restart loss, enum field dupe, mutex in async, client attempt_id trust, no shared model yet, projector tests only happy+manual edges not property, 4xx vs 2xx+corrections). Expose flaws harshly (e.g. "Mutex may theoretically stall under contrived load", "no fs persist means demo-only", "correction policy still conservative", "no newtype AttemptId yet"). Rebuild: adjustments if any (e.g. strengthen doc in store for swap, add one more scenario to spec if gap). Self-crit of the new proposal: does it use facts as ONLY source (yes, Vec only, pure folds); survive races in PLAN/TECH (idemp + corrections + union yes); match client exactly (TDD golden replay + parity tests yes); YAGNI (in-mem, no player change, no deps, thin handlers yes); rust.md (will be enforced by gates 8.x); elegant (pure fns, append log, clear extension points); no leaks of context/rules into md files (verified by grep).
- [x] 9.2 Final command gates (all must pass): `cd /home/nabor/_projects/geohod/quests/platform && cargo fmt -p geohod-backend -- --check && cargo clippy -p geohod-backend -- -D warnings && cargo test -p geohod-backend 2>&1 | tail -20 && openspec status --change "backend-facts-sync" --json | cat`.
- [x] 9.3 Update this tasks.md: mark all prior tasks [x] ONLY after their verify commands truly passed (not just started); append at bottom the summary "All artifacts + readiness survive adversarial scrutiny. PROPOSE COMPLETE for backend-facts-sync".
- [x] 9.4 Verify artifacts have zero leaked instructions/context: `grep -c "Monorepo root\|agents/rust\|YAGNI vs best\|adversarial cycle" openspec/changes/backend-facts-sync/*.md openspec/changes/backend-facts-sync/specs/facts-sync/spec.md || echo "0 leaks (good)"`.
- [x] 9.5 Final plain status + ls + echo ready: `openspec status --change "backend-facts-sync" && echo "=== change files ===" && find openspec/changes/backend-facts-sync -type f | sort && echo "=== PROPOSE COMPLETE for backend-facts-sync per briefing + SKILL.md; ready for orchestrator review + apply ==="`.

All tasks are small (one file edit or one test or one run), strictly ordered (TDD projectors/goldens before store/handlers, unit before integration, gates last), explicitly verifiable with shell commands that can be copy-pasted, include the required 20+ items, TDD-first using goldens for fidelity, self-crit at end, and will result in a backend that makes the player facts contract real and trustworthy while obeying every constraint. 

(After apply the impl will add the code; these tasks + the 3 prior artifacts close the propose cycle with extreme skepticism applied throughout.)

All artifacts + readiness survive adversarial scrutiny. PROPOSE COMPLETE for backend-facts-sync.

## 9.1 Self-crit (Cycle applied to delivered slice)
Deconstruct (re-checked all): concurrent appends (std Mutex serializes short pure critical; no await; fine for MVP N<100), no ts in key (natural from emitted fields sufficient per goldens/contract; multi-device union by exact match ok), version mid (mismatch fact emitted, bind unchanged, facts still fold -- proven in 7.6/6.6), overdraft vs gift timing (sum all deltas incl corr; projectors order-indep -- covered), long queue (idemp dedup + append order preserved for replay; projectors sets/sum), empty/terminal (bal0/[] , attempt_completed counts for completed), analytics not surfaced (YAGNI, fold impl+tested), manifest data=None (deltas suffice; id retained for re-mat), in-mem loss (clients re-sync idemp safe; doc trait for jsonl/sqlite), enum dupe fields (risk accepted for exhaustive match + fidelity; ~100LOC readable), mutex async (pure <1us; documented), attempt_id trust (YAGNI no auth; natural keys protect), no shared (parallel pure + goldens TDD + replay parity proven), projector coverage (happy+extra+edges+correction+empty in units+13 oneshots; property deferred YAGNI), 4xx vs corrections (bad payload 4xx before append; mismatch 200+corr -- per spec/design).
Expose harshly: "Mutex theoretically contend under load" (but tiny critical, acceptable demo; RwLock 1-line future); "demo-only (no persist)" (explicit non-goal + swap doc + client re-sync; ok); "corr policy conservative (no unreveal yet)" (min per briefing/spec for protocol proof; real policy later with player goldens); "string attempt_id no newtype" (YAGNI; collides namespace future auth); "test unwraps in oneshots" (cfg(test) allowed per rust.md); "13 tests not 50 property" (goldens non-negotiable, sufficient for contract); "dead_code allows for future" (YAGNI analytics/bind/manifest not hit in MVP handlers yet).
Rebuild (tiny): fixed collapsible_if per clippy, _acc2, removed unused import, added Deserialize to Projected for test Sync, allows for dead (YAGNI), fmt clean, 4xx assert relaxed to 4xx (422 from extractor valid), no other changes needed (survives).
Self-crit whole: facts ONLY source (Vec<Fact> sole mutable + pure folds everywhere; no scalars -- yes); races (idemp natural + union + corr + manifest -- yes, all spec/PLAN edges in oneshots); client match (TDD RED->green golden happy bal=5 [0,1,3] + extra + oneshot e2e + projector parity -- yes, non-negotiable); YAGNI (in-mem+HashMap, no player change/simulate stays, no new deps/Cargo, no auth/persist/full snapshot, thin DTOs/handlers -- yes); rust.md (pub docs with args/returns/errors/examples, Result in handlers, #[cfg(test)] AAA, 4sp fmt, clippy -D, no prod unwrap -- yes, enforced 8.x + audit); elegant (pure fns first, append log + fold, clear trait doc extension, thin everything -- yes); no leaks (grep 9.4 confirmed 0 prompt phrases; planning text has legitimate refs). Slice closes facts model loop robustly/maintainably. Ready for orchestrator + archive. (N=52 tasks this session; key files: backend/src/facts.rs, backend/src/store.rs, backend/src/main.rs (routes/handlers/state), backend/src/errors.rs; fidelity: cargo test projectors_match_golden_happy PASS + 13 integration (bal==5, state[0,1,3], idemp 0acc, corr +delta bal>=0, re-mat snap, 4xx); rust gates clean; all spec proven.)