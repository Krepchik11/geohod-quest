## Why

After the quest-player-pwa + backend-facts-sync + player-reconnect-wiring cycles (4-template player, append-only facts with idempotent natural keys + corrections + manifest, client/server projector fidelity on real export goldens including "МИХАЙЛО ПУПИН" answer + gift +5 + edges, real reconnect wiring making simulate use the contract, 100% shared, TDD goldens), Phase 3 Commerce/Marketplace is required per PLAN. Lifetime AccessGrant (idempotent, source-audited: Payment/CouponRedemption/FreeQuest/Admin) + single-quest checkout + coupon % (incl 100%) + free quests (identical mechanics) + peer Marketplace surface (lists published quests with primary comic + template summary from snapshot; publish flow surfaces; grants/purchases flow through) close the "buy once / lifetime access" loop. Grants/facts/snapshots enable owned checks + post-grant attempt eligibility while reusing snapshots for listing/owned and facts for usage. Explicit cuts observed (no real-money, no recurring, no cart, no external authors). TDD goldens extended for grant+attempt+coupon flows; YAGNI in-mem/simple stores.

## What Changes

- Add lifetime AccessGrant (player + quest, source-audited enum, idempotent by (player, quest) with source recorded for audit; survives version publishes).
- Single quest checkout endpoint/stub + coupon % input (100% coupon treated as free source=CouponRedemption; free quests use identical grant/attempt mechanics with source=FreeQuest).
- Marketplace peer surface: lists published quests (primary comic + 4-template summary derived from snapshot), "buy once" or "get free" flows that create grant through the surface.
- Ctor publish surfaces to marketplace (registers snapshot id + summary for peer listing; no god objects).
- Reuse 100% existing snapshots (for published list + owned/comic/template summary), facts/projectors (for post-grant usage/attempt eligibility), goldens (extend replay for grant creation idemp + attempt post-grant + coupon redemption + free identical).
- Enhance existing small components + new narrow ones (MarketplaceList, CheckoutForm with coupon, PublishSurface integration); no new packages, no bloat.
- TDD goldens first (RED for grant/attempt/coupon flows, then green); backend in-mem grants store (idemp creation, explicit extension point for persist like facts); client/server invariants (grant before attempt facts; snapshot frozen at publish time).
- **Non-changes (YAGNI per PLAN/explicit cuts)**: no real-money payments/coin purchases, no recurring subscriptions, no multi-quest cart, no external authors, no deep coupon validation/limits/store (simple % or code input), no high-fidelity preview, in-mem grants (documented swap path), no magic links, basic normalization only. Demo player_id fixed ("demo-player"); attempt eligibility enforced client+server stub using grant or free flag.
- No **BREAKING** changes (additive to shared Fact/QuestSnapshot, existing routes/players/ctors continue to work; goldens fidelity preserved).

## Capabilities

### New Capabilities
- `marketplace-grants`: Lifetime AccessGrant (idempotent creation, source-audited Payment/CouponRedemption/FreeQuest/Admin); single-quest checkout with coupon % (incl. 100% = free-equivalent); free quests using identical mechanics (grant + attempts + snapshots + facts); peer Marketplace surface listing published quests (primary comic + template summary from snapshot data); publish flow surfaces published quests to marketplace; grants and purchases flow exclusively through the marketplace surface; attempt eligibility requires valid grant or free quest (reuses player/facts post-grant); TDD goldens extended for grant creation (idempotent), post-grant attempt, coupon redemption, free identical flows. (Creates `specs/marketplace-grants/spec.md`)

### Modified Capabilities
- `quest-player-pwa`: Attempt creation/eligibility and fact append now require a valid lifetime AccessGrant for the quest (or free quest flag); "owned" status surfaced from grants for replay/resume; eligibility checks reuse snapshots for version binding and facts for post-grant usage/attempts (additive requirements; existing 4-template, facts, reconnect, goldens fidelity unchanged). (Delta at `specs/quest-player-pwa/spec.md`)

## Impact

- **Frontend**: `frontend/app/page.tsx` (enhance Marketplace card/section into functional peer list with comic + template summary from snapshots, buy/get-free flows); small focused components (MarketplaceList, CheckoutForm w/ coupon %, owned indicators; enhance PublishSurface in ctor integration); `frontend/lib/shared-model.ts` (additive AccessGrant interface + pure eligibility helpers, 100% reuse for snapshots/facts); `frontend/lib/__tests__/player-replay.test.ts` + `frontend/lib/goldens.ts` (extend goldens-driven replay for grant creation idemp + attempt post-grant + coupon 100% + free identical; RED first then green); `frontend/app/constructor/page.tsx` (enhance publish to surface snapshot to marketplace via backend or contract); `frontend/app/quest/QuestPlayerClient.tsx` + related (additive eligibility gate on start/append using grant or free, narrow; preserve all prior flows).
- **Backend**: `backend/src/store.rs` (extend or parallel InMemoryGrantStore with idemp create by (player,quest) + source audit; explicit trait/extension point for future persist); `backend/src/grants.rs` (new thin module for Grant enum/types + pure projectors if needed, per rust.md); updates to `backend/src/main.rs` (new routes /api/quests, /api/checkout, /api/grants, /api/quests/publish; AppState with grants store behind Arc<Mutex>; integrate with facts for eligibility in future append); `backend/src/errors.rs` (additive if needed); `backend/src/facts.rs` (no change or tolerant); new integration tests in #[cfg(test)] for grant idemp, coupon 100%, concurrent sim, post-grant facts eligibility, replay goldens cases.
- **OpenSpec + quality**: New `proposal.md`, `design.md`, `specs/marketplace-grants/spec.md`, `specs/quest-player-pwa/spec.md` (delta), `tasks.md`; on apply, syncs specs to `openspec/specs/`. 20+ small TDD tasks (reads first, goldens RED first, gates: replay green + grant cases, cargo test/clippy/fmt, frontend build/lint, reuse greps, manual "buy once" + attempt post-grant + coupon + free + publish surface + reconnect). No new deps.
- **Downstream**: Enables real commerce loop per PLAN after Player+Sync (buy surfaces published snapshot; grant before attempt; lifetime; peer marketplace); unblocks admin visibility (grants per version); "Save + open in real player" from ctor now can target granted quests; all invariants (snapshots frozen, facts append-only, client authoritative) preserved.
- Follows agents/rust.md + react.md (docs, no unwrap, small comps, no waterfalls, 100% shared imports, derived state, explicit), TDD/SOLID/DRY/KISS/YAGNI, blueprint (Commerce bullets + cuts + risks), prior artifacts (reuse snapshots/facts/goldens exactly, enhance not rewrite). All client/server contracts (grant before attempt, snapshot at publish, idemp) survive races (concurrent buy, coupon double, publish mid-checkout, multi-device grant, historical snapshot for granted attempt).
- No scope creep; additive only.

This change makes lifetime "buy once" access + peer marketplace real and verifiable against the contract after the prior cycles. All artifacts produced via full adversarial cycle (deconstruct/expose/rebuild/self-crit on every decision). Ready for apply after review.