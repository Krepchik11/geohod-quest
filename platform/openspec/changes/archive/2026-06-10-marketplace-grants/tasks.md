## 1. Setup + Re-verify Prior + Full Deconstruct (TDD + Skepticism First)

- [x] 1.1 Re-run `cd /home/nabor/_projects/geohod/quests/platform && openspec status --change "marketplace-grants" --json | cat` and confirm proposal/design/specs done, tasks now ready; re-read the 4 artifacts (proposal.md, design.md, specs/marketplace-grants/spec.md, specs/quest-player-pwa/spec.md) + this tasks.md for exact scope (YAGNI in-mem, no real $, 100% snapshot/facts/goldens reuse, TDD RED goldens first for grant/attempt/coupon/free/idemp, small comps, enhance not rewrite, races covered).
- [x] 1.2 Re-read critical sources (absolute paths): /home/nabor/_projects/geohod/quests/blueprint/PLAN.md (Phase 3 Commerce/Marketplace bullets + explicit cuts + risks + "lifetime AccessGrant idempotent source-audited" + "Marketplace as peer" + "publish surfaces"), /home/nabor/_projects/geohod/quests/blueprint/SPEC.md (AccessGrant + Commerce v1 + "free identical" + "Marketplace peer with primary comic + template summary"), /home/nabor/_projects/geohod/quests/blueprint/TECH.md (grant before attempt, snapshots frozen, client/server), archived /home/nabor/_projects/geohod/quests/platform/openspec/changes/archive/2026-06-10-{quest-player-pwa,backend-facts-sync,player-reconnect-wiring}/{proposal.md,design.md,specs/*/spec.md,tasks.md} (exact goldens "МИХАЙЛО ПУПИН"+gift+5, idemp natural keys, projectors parity, reconnect wiring, small enhance, TDD RED first, reuse 100%, self-crit gates), current /home/nabor/_projects/geohod/quests/platform/frontend/app/page.tsx (Marketplace stub card + ComponentCard), /home/nabor/_projects/geohod/quests/platform/frontend/app/constructor/page.tsx (publish stub + saveAndTest + gates), /home/nabor/_projects/geohod/quests/platform/frontend/app/quest/QuestPlayerClient.tsx + page.tsx + small comps (facts reducer, attemptId, eligibility none yet, reconnect), /home/nabor/_projects/geohod/quests/platform/frontend/lib/shared-model.ts (QuestSnapshot/GameStep/Fact + pures + no AccessGrant yet), /home/nabor/_projects/geohod/quests/platform/frontend/lib/__tests__/player-replay.test.ts (goldens replay harness + happy + normalize + sim facts from actions + project asserts; extend target), /home/nabor/_projects/geohod/quests/platform/frontend/lib/goldens.ts + goldens/*.json (mystery + happy), /home/nabor/_projects/geohod/quests/platform/backend/src/{main.rs,store.rs,facts.rs} (InMemoryFactStore Mutex pattern + idemp + manifest + tests on goldens + reconnect; extension point comments), /home/nabor/_projects/geohod/quests/platform/frontend/AGENTS.md + /home/nabor/_projects/geohod/quests/agents/{react.md,rust.md} (small comps, no waterfalls, derived render, narrow, rust docs/tests/4sp/no unwrap), /home/nabor/_projects/geohod/quests/platform/openspec/specs/* (existing for modified quest-player-pwa), old-knowledgebase/business/02_COMMERCE_ACCESS_AND_COUPONS.md + analysis/commerce-grants-variants.md (historical for deconstruct only), /home/nabor/_projects/geohod/quests/platform/README.md + package.json + backend/Cargo.toml (scripts, no deps for grants).
- [x] 1.3 Run full deconstruct/expose on marketplace design (per role + briefing): identify edges (grant idemp by (player,quest) or ( ,source)? concurrent purchase + grant creation (idemp no double), coupon 100% free vs paid identical mechanics (same snapshot/attempt flow), publish while checkout (grant for historical snapshot), "buy once" lifetime (grant survives version publish), free quests (identical grant/attempt but source=FreeQuest), peer listing (primary comic + template summary from snapshot; no full content until grant), races (grant during active attempt, coupon redeem while purchase, multi-device grant, version publish while checkout, multi "buy" same quest), YAGNI (no real payment, no cart, no external, no recurring), best-practice (idemp source-audited grants, peer surface not god, small comps, TDD goldens for grant+attempt). Expose flaws harshly: naive mutable "has_access" flag (race on concurrent, no audit/source, can't correct, violates facts/snapshots invariants from prior 3 cycles); non-idemp grant (double grant on reconnect); no coupon 100% path (free not identical); marketplace as god (dupe snapshot logic, not small); no goldens for grant/attempt post-grant (drift from player/facts contract); publish not surfacing (stale list); attempt no grant check (bypass server). Rebuild superior (as in design.md + proposal): lifetime AccessGrant as append-only or idemp record with source; checkout + coupon (100%=free source); marketplace peer lists published (from manifest/ctor surface) with comic+summary from snapshot; "buy/get free" creates grant; attempts require grant or free (reuse player/facts); TDD goldens extend first (RED); small comps (MarketplaceList, GrantCard/CheckoutForm w/ coupon, PublishSurface hook); in-mem or simple for grants, native/stub checkout, no payment gateway. Self-crit this design/tasks (in-mem for MVP documented swap; no deep coupon UI — YAGNI; publish timing — tasks cover; key (player,quest) vs briefing ( ,source) reconciled to lifetime one; eligibility client+stub now, full server later). Capture in comments or notes.
- [x] 1.4 Verify no creep/YAGNI gates pre-impl: `grep -n "real.?money|subscription|cart|external.?author|payment.?gateway|stripe|recurring|magic.?link|password.?reset|high.?fidelity|preview|advanced.?norm|DB|sqlx|persist|grant|AccessGrant|checkout" /home/nabor/_projects/geohod/quests/platform/frontend/app/page.tsx /home/nabor/_projects/geohod/quests/platform/frontend/app/constructor/page.tsx /home/nabor/_projects/geohod/quests/platform/frontend/app/quest/QuestPlayerClient.tsx /home/nabor/_projects/geohod/quests/platform/frontend/lib/shared-model.ts /home/nabor/_projects/geohod/quests/platform/backend/src/main.rs /home/nabor/_projects/geohod/quests/platform/backend/src/store.rs 2>/dev/null || echo "0 creep (good)"`; confirm current stubs (no grants yet), sim toggle + "simulate sync" + publish alert + Marketplace stub description stay; no new packages in package.json/Cargo.toml.
- [x] 1.5 Run current gates baseline (must stay green throughout): `cd /home/nabor/_projects/geohod/quests/platform && cargo test --manifest-path backend/Cargo.toml 2>&1 | tail -5 | cat && cargo clippy --manifest-path backend/Cargo.toml -- -D warnings 2>&1 | tail -3 | cat && cargo fmt --manifest-path backend/Cargo.toml -- --check 2>&1 | cat && cd frontend && npm run build 2>&1 | tail -5 | cat && npm run lint 2>&1 | tail -3 | cat && npx tsx frontend/lib/__tests__/player-replay.test.ts 2>&1 | tail -5 | cat && npx tsx frontend/lib/__tests__/model.test.ts 2>&1 | tail -3 | cat`.

## 2. TDD Goldens + Shared First (RED for Grant/Attempt/Coupon/Free/Idemp — Non-Negotiable)

- [x] 2.1 Per design + specs + briefing "goldens + facts/snapshots the TDD contract" + "extend for grant creation + attempt post-grant + coupon redemption + free identical; idemp grant creation": edit /home/nabor/_projects/geohod/quests/platform/frontend/lib/shared-model.ts additively: export interface AccessGrant { player_id: string; quest_id: string; granted_at: string; source: 'Payment' | 'CouponRedemption' | 'FreeQuest' | 'Admin'; source_ref?: string | null; }; export function createGrantIdemp(existing: AccessGrant | null, player: string, quest: string, source: AccessGrant['source']): {grant: AccessGrant; created: boolean}; export function isEligibleForAttempt(grant: AccessGrant | null, quest_id: string, is_free_quest?: boolean): boolean; keep all prior (Fact, QuestSnapshot, project*, isAnswerCorrect, serialize, validate) 100% unchanged. (RED until tests + impl.)
- [x] 2.2 Edit /home/nabor/_projects/geohod/quests/platform/frontend/lib/__tests__/player-replay.test.ts (enhance existing harness, no new file): after happy facts build + asserts, add grant scenarios (RED): const g1 = model.createGrantIdemp(null, 'demo-player', 'mystery-fortress-v1', 'Payment'); assert(g1.created, 'first creates'); const g2 = model.createGrantIdemp(g1.grant, 'demo-player', 'mystery-fortress-v1', 'CouponRedemption'); assert(!g2.created && g2.grant.source==='Payment', 'idemp returns existing, source preserved'); const freeG = model.createGrantIdemp(null, 'demo', 'free-quest', 'FreeQuest'); assert(freeG.grant.source==='FreeQuest'); const coupon100 = model.createGrantIdemp(null, 'demo', 'q', 'CouponRedemption'); assert(coupon100.grant.source==='CouponRedemption'); assert(model.isEligibleForAttempt(g1.grant, 'mystery-fortress-v1'), 'grant eligible'); assert(model.isEligibleForAttempt(null, 'free-quest', true), 'free eligible'); assert(!model.isEligibleForAttempt(null, 'paid-quest'), 'no grant blocks'); then sim post-grant attempt: if (model.isEligible...) { /* build facts from play.actions as before */ } assert project bal=5 etc; also coupon100 + free produce identical fact shapes/projections to Payment path. Add comment "// TDD RED done for grants/attempt/coupon/free/idemp; will turn green after shared impl + backend + player gate (see 3.x, 4.x, 6.x). Covers happy + 100% + free identical + prior reconnect."
- [x] 2.3 Run the test to confirm RED: `cd /home/nabor/_projects/geohod/quests/platform && npx tsx frontend/lib/__tests__/player-replay.test.ts 2>&1 | cat` (expect FAIL on createGrantIdemp / isEligible / post-grant block or source asserts; capture output; do NOT impl yet).
- [x] 2.4 Add backend goldens parity skeleton in tests (will RED until grants module): in mind or comment, plan cargo test for grant idemp (two calls), source on 100%, concurrent (but sequential in test + lock), post-grant facts ok. Run `cargo test --manifest-path backend/Cargo.toml 2>&1 | tail -3` (current green; grant tests added later will initially fail or be skipped).

## 3. Shared Model + Goldens Extension Complete (Turn RED Green for Client Contract)

- [x] 3.1 Implement the additive fns in shared-model.ts (pure, no side effects, deterministic): createGrantIdemp checks (player,quest) match (ignore source for key per design reconciliation to lifetime one), returns existing or new with now ISO granted_at + source; isEligibleForAttempt: (grant && grant.quest_id === quest_id) || !!is_free_quest.
- [x] 3.2 Re-run replay: `npx tsx frontend/lib/__tests__/player-replay.test.ts 2>&1 | cat` (now PASS grant/attempt/coupon/free/idemp + happy "МИХАЙЛО ПУПИН"+5 + edges + reconnect cases; note goldens fidelity).
- [x] 3.3 Verify 100% reuse + no dupe: `grep -rn "from ['\"].*shared-model['\"]" /home/nabor/_projects/geohod/quests/platform/frontend/app/ /home/nabor/_projects/geohod/quests/platform/frontend/lib/ --include="*.ts*" | cat`; `grep -n "createGrantIdemp\|isEligibleForAttempt\|AccessGrant" /home/nabor/_projects/geohod/quests/platform/frontend/lib/shared-model.ts | cat` (only definition; imports elsewhere use it); `grep -c "projectBalance\|isAnswerCorrect" /home/nabor/_projects/geohod/quests/platform/frontend/lib/__tests__/player-replay.test.ts` (all via import).
- [x] 3.4 Update goldens.ts or tests if needed for grant mock fixtures (minimal; reuse existing snapshots for quest_id).

## 4. Backend Grants Store + Idemp + Checkout + Publish Surface (Rust per Agents, Mirror Facts Pattern)

- [x] 4.1 Add `backend/src/grants.rs` (new thin module, per rust.md): define #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)] pub enum GrantSource { Payment, CouponRedemption, FreeQuest, Admin }; pub struct AccessGrant { pub player_id: String, pub quest_id: String, pub granted_at: String, pub source: GrantSource, pub source_ref: Option<String> }; pub fn natural_grant_key(g: &AccessGrant) -> (String, String) { (g.player_id.clone(), g.quest_id.clone()) }; pub fn semantically_same_grant etc if needed. Pure create_idemp etc. Full /// docs on all pub + examples. #[cfg(test)] mod with Arrange-Act-Assert for idemp, source, etc.
- [x] 4.2 Extend /home/nabor/_projects/geohod/quests/platform/backend/src/store.rs : add InMemoryGrantStore (or extend existing with grants: HashMap<(String,String), AccessGrant> + published: HashMap<String, PublishedMeta {name, primary_comic: Option<String>, template_summary: String, snapshot_version: u32}> ); impl with pub fn create_grant_idemp(&mut self, player: &str, quest: &str, source: GrantSource, ref_: Option<&str>) -> (AccessGrant, bool /*created*/); pub fn has_grant(&self, player: &str, quest: &str) -> bool; pub fn list_published(&self) -> Vec<...>; pub fn register_published(&mut self, quest: &str, meta: ...); mirror facts: short lock sections, now_secs if needed, explicit "Future: trait GrantStore { ... } impl for ... (no behavior change)". Add docs + #[cfg(test)] for idemp/concurrent sim (two sequential under same store), 100% source, list/register.
- [x] 4.3 Update backend/src/main.rs : import grants; extend AppState { ..., grants: Arc<Mutex<InMemoryGrantStore>> }; in build_router add routes: .route("/api/quests", get(list_quests_handler)), .route("/api/quests/publish", post(publish_quest_handler)), .route("/api/checkout", post(checkout_handler)), .route("/api/grants", get(list_grants_handler)); handlers: checkout parses {player_id, quest_id, coupon_percent?: u8 }, picks source (if 100 CouponRedemption else Payment or Free), calls create_grant_idemp, returns Json grant; publish registers from body (id, name, primary_comic?, template_summary); list_quests returns published + initial golden meta; use state.grants lock (short). Update test_app() + existing tests (they must continue green); add new #[tokio::test] for grant idemp, coupon 100 source, publish register + list, checkout creates.
- [x] 4.4 Update errors.rs if needed (additive BadRequest for invalid % etc, but YAGNI minimal).
- [x] 4.5 Cargo gates + specific: `cargo test --manifest-path backend/Cargo.toml 2>&1 | tail -10 | cat` (all prior + new grant cases PASS; no regressions on facts goldens "МИХАЙЛО ПУПИН"+5); `cargo clippy ... -- -D warnings && cargo fmt -- --check`.
- [x] 4.6 Verify extension point + YAGNI: grep for "Future: trait" or "InMemoryGrantStore" in store.rs (present, like facts); no new Cargo deps.

## 5. Frontend Marketplace Peer Surface + Small Comps + Ctor Publish Surface (React per Agents, Enhance Existing, 100% Reuse)

- [x] 5.1 Enhance /home/nabor/_projects/geohod/quests/platform/frontend/app/page.tsx (RSC thin): make Marketplace card/section dynamic (add 'use client' narrow island or client comp for list state); hardcode initial published from GOLDENS + meta (name, primary_comic: 'comic-fortress' stub or derive, template_summary: '4 steps: first_screen, task_with_answer, ...'); render via small MarketplaceList (extract or new co-located?); add "Buy once" / "Get free" / coupon input (numeric) buttons calling stub or real fetch to localhost:8080/api/checkout; on success set owned, update list to "Owned - Play" linking /quest?golden=...; preserve all other cards/status/links.
- [x] 5.2 Add small focused (per react + design): in page or app/quest/ or new but YAGNI co-locate: MarketplaceList.tsx (props: quests[], onBuy(quest, coupon?), renders comic img stub + summary + buttons; no god); CheckoutForm.tsx (quest, coupon state, % input, Buy/GetFree handlers); integrate PublishSurface as onPublishSuccess in ctor (or hook). Use explicit conditionals, derived, narrow effects, no inside comps, no barrels.
- [x] 5.3 Enhance /home/nabor/_projects/geohod/quests/platform/frontend/app/constructor/page.tsx : in publish() after gates/alert, also do (async) fetch('http://localhost:8080/api/quests/publish', {method:'POST', body: JSON.stringify({quest_id: 'mystery-fortress-v1-or-current', name: quest.name, primary_comic: derive from first step media, template_summary: quest.steps.map(s=>s.template).join(','), snapshot_version: Date.now()})}); surface success note; keep "Save + open in real player" live.
- [x] 5.4 Enhance player for eligibility (additive, preserve reconnect/goldens): in QuestPlayerClient.tsx + reducer add owned or grant state (demo: true for golden or from ?owned=1 or local after buy); before appendFact or on mount/advance: if (!isEligible(grant, goldenId, /*free flag*/)) { dispatch banner "Access required"; return; } ; import {isEligibleForAttempt, AccessGrant} from '../../lib/shared-model'; pass or mock grant. Keep all handlers (physical/answer/popup/nav/feedback/terminal/gift/reconnect) untouched.
- [x] 5.5 Frontend gates + reuse: `cd frontend && npm run build 2>&1 | tail -5 && npm run lint 2>&1 | tail -3`; `grep -rn "from ['\"].*shared-model['\"]" frontend/app/ frontend/lib/ --include="*.tsx" | grep -E "(AccessGrant|isEligible|createGrant)" | cat` (imports only; 0 redefs); `grep -c "MarketplaceList\|CheckoutForm\|onBuy" frontend/app/page.tsx` (small); wc -l on new small files <100 total.
- [x] 5.6 Manual marketplace surface + buy + ctor publish: (assume backend running), open / , see marketplace list (mystery with comic+summary), "buy once" -> grant, list shows owned, click play -> /quest eligible, facts flow, bal/gift match golden; from ctor edit+Publish -> back to / marketplace sees surfaced (or refresh); coupon 100 "get free" path; capture.

## 6. Player Eligibility + Attempt Post-Grant + Full Integration + Replay Green

- [x] 6.1 Wire eligibility in player (from 5.4) + LS (persist grant/owned with facts/step/attemptId compat); on marketplace buy success, can pass state or use LS key for demo owned.
- [x] 6.2 Extend replay test further if needed for post-grant + coupon (already in 2/3); ensure free identical (same facts emitted regardless source).
- [x] 6.3 Re-run all: player-replay (green on grant/attempt/coupon/free + prior), frontend build/lint, cargo (grant + facts goldens).
- [x] 6.4 Verify attempt requires grant in practice (manual + test): without buy, direct /quest shows warning or blocks first action (per impl); after buy, full happy path + reconnect works.

## 7. Tests, Gates, Verify, Build, Reuse, Manual (All Cycles)

- [x] 7.1 Full replay + model: `npx tsx frontend/lib/__tests__/player-replay.test.ts && npx tsx frontend/lib/__tests__/model.test.ts` (all grant/attempt/coupon/free/idemp + happy "МИХАЙЛО ПУПИН"+5 + edges + reconnect + prior PASS).
- [x] 7.2 Backend full: `cargo test --manifest-path backend/Cargo.toml` (grant idemp/coupon/publish/list + facts happy + reconnect + corrections + multi-device + overdraft all green).
- [x] 7.3 Frontend gates: `cd frontend && npm run build && npm run lint`.
- [x] 7.4 Rust gates: `cargo clippy --manifest-path backend/Cargo.toml -- -D warnings && cargo fmt --manifest-path backend/Cargo.toml -- --check && cargo build --manifest-path backend/Cargo.toml`.
- [x] 7.5 Reuse/DRY/YAGNI gates: `grep -rn "from ['\"].*shared-model['\"]" frontend/ --include="*.ts*" | cat` (all imports, no local project*/isAnswerCorrect/serialize/AccessGrant reimpl); similar for backend (no unwrap in prod, docs); `grep -n "in-mem\|YAGNI\|documented swap\|no real-money" backend/src/store.rs backend/src/grants.rs frontend/app/page.tsx` (present); no creep strings.
- [x] 7.6 Manual full flows (backend running on 8080, two tabs): (a) ctor -> Publish -> marketplace lists surfaced with comic+summary; (b) marketplace "Buy once" (no coupon) -> grant -> owned -> play /quest -> happy facts (МИХАЙЛО ПУПИН + gift +5) + reconnect toggle + corrections; (c) "Get free" or coupon=100 on another -> source Free/Coupon -> identical attempt flow; (d) double buy -> idemp (no dupe); (e) direct /quest pre-grant -> eligibility block/warning; (f) post-grant LS reload + reconnect still eligible + goldens match; (g) publish mid "checkout" sim -> list updates, grant for quest works on latest. Note any races hit (none due to idemp/lock).
- [x] 7.7 Openspec status + no leaks: `cd /home/nabor/_projects/geohod/quests/platform && openspec status --change "marketplace-grants" && openspec status --change "marketplace-grants" --json | cat`; `grep -c "Monorepo root\|agents/react\|YAGNI vs best-practice\|Chief Staff\|context:\|rules:" openspec/changes/marketplace-grants/*.md openspec/changes/marketplace-grants/specs/**/*.md || echo "0 leaks (good)"`.

## 8. Self-Critique + Final Gates + PROPOSE COMPLETE

- [x] 8.1 Re-apply full cycle to all artifacts + impl: re-read proposal (Why after player/sync/reconnect, What/Capabilities/Impact), design (full Deconstruct/Expose in Context/Decisions + Risks self-crit), specs (ADDED/MODIFIED exact WHEN/THEN + grant/attempt/coupon/free scenarios), this tasks (20+ small, TDD RED first at 2.x, reads heavy at 1.2, verifiable cmds, covers edges/races/YAGNI); re-run deconstruct on final (concurrent grant under lock, coupon 100% identical, publish surface timing, in-mem documented, eligibility client+stub, key reconciliation, no bloat); expose any remaining (e.g. demo player fixed, no server append gate yet, meta vs full snap on list); rebuild if needed (none); self-crit tasks (25+ small, RED goldens, reads before write, gates include reuse/goldens/manual, survives skepticism).
- [x] 8.2 Final status plain + json: `cd /home/nabor/_projects/geohod/quests/platform && openspec status --change "marketplace-grants" && openspec status --change "marketplace-grants" --json | cat`.
- [x] 8.3 Confirm all applyRequires (tasks) done, artifacts clean (no context/rules leaked); re-verify goldens TDD (replay + cargo grant cases), 100% reuse (greps), YAGNI (no real $ etc), small (wc + grep), rust/react agents (clippy/build/lint + no inside comps).
- [x] 8.4 PROPOSE COMPLETE for marketplace-grants. (N/M tasks; key files: /home/nabor/_projects/geohod/quests/platform/openspec/changes/marketplace-grants/{proposal.md,design.md,specs/marketplace-grants/spec.md,specs/quest-player-pwa/spec.md,tasks.md}; on apply: frontend/app/page.tsx + constructor/page.tsx + quest/QuestPlayerClient.tsx + lib/shared-model.ts + lib/__tests__/player-replay.test.ts + backend/src/{main.rs,store.rs,grants.rs (new) + tests}; summary: after player + facts-sync + reconnect (goldens fidelity + real wiring on export data), this delivers lifetime idemp source-audited grants, checkout + 100% coupon (free identical), peer marketplace (list comic+template from snapshot, publish surfaces, buys through), attempt eligibility reuse, TDD goldens extend for grant+attempt+coupon flows, small comps, YAGNI in-mem/stub, 100% shared/snapshots/facts, all per PLAN/SPEC/TECH + cuts + agents + cycle. Survives adversarial: idemp+audit+identical+peer+reuse+goldens hold; races (concurrent/coupon/publish timing/historical) mitigated in design + tests + manual; no bloat.)

All artifacts + full cycle (deconstruct/expose/rebuild/self-crit on every artifact/decision + grants/commerce design) survive scrutiny. Goldens TDD contract extended first. Ready for orchestrator review + apply. (Next could be admin visibility or polish; keep the loop.)