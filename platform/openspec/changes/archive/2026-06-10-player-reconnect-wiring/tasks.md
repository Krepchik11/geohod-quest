## 1. Setup + Re-verify Prior + Deconstruct (TDD + Skepticism First)

- [x] 1.1 Re-run `cd /home/nabor/_projects/geohod/quests/platform && openspec status --change "player-reconnect-wiring" --json | cat` and confirm proposal/design/specs done, tasks now ready; re-read the 3 artifacts (proposal.md, design.md, specs/quest-player-pwa/spec.md) + this tasks.md for exact scope (YAGNI, no grants, sim preserved, 100% shared reuse, goldens drive reconnect cases).
- [x] 1.2 Re-read critical sources (absolute paths): /home/nabor/_projects/geohod/quests/blueprint/PLAN.md (Phase 3 Player reconnect bullets + "facts mandatory" + risks), /home/nabor/_projects/geohod/quests/blueprint/SPEC.md (facts, offline), /home/nabor/_projects/geohod/quests/blueprint/TECH.md (reconnect upload + corrections), archived /home/nabor/_projects/geohod/quests/platform/openspec/changes/archive/2026-06-10-backend-facts-sync/{proposal.md,design.md,specs/facts-sync/spec.md} (exact POST body/response SyncResponse, natural keys, overdraft corr scenario with balance_corrected, goldens fidelity, "client can append ... corrections"), archived /home/nabor/_projects/geohod/quests/platform/openspec/changes/archive/2026-06-10-quest-player-pwa/{design.md,specs/quest-player-pwa/spec.md} (simulate stub contract, LS key `quest-player-${goldenId}`, reducer, banner, 100% import shared), current /home/nabor/_projects/geohod/quests/platform/frontend/app/quest/QuestPlayerClient.tsx + OfflineBanner.tsx + page.tsx (handleSimSync alert, facts reducer, appendFact, LS effects, derived project* in render, <OfflineBanner onSimSync=...>), /home/nabor/_projects/geohod/quests/platform/frontend/lib/shared-model.ts (current Fact 7-type interface + projectBalance sum + projectState fold), /home/nabor/_projects/geohod/quests/platform/frontend/lib/__tests__/player-replay.test.ts (goldens replay harness + happy "МИХАЙЛО ПУПИН"+5 + edges + LS), /home/nabor/_projects/geohod/quests/platform/frontend/goldens/playthrough-happy-with-gift.json (expected_facts), /home/nabor/_projects/geohod/quests/platform/frontend/AGENTS.md + /home/nabor/_projects/geohod/quests/agents/react.md (RSC thin, small 'use client', narrow effects, derived in render, explicit, no inside-defs), /home/nabor/_projects/geohod/quests/platform/backend/src/main.rs + facts.rs (contract AppendRequest/SyncResponse, corr variants in Fact enum, /api/attempts/{id}/facts + /state), /home/nabor/_projects/geohod/quests/platform/openspec/specs/quest-player-pwa/spec.md + facts-sync/spec.md (current simulate req + reconnect expectation to update).
- [x] 1.3 Run full deconstruct/expose on wiring (per role): identify races (append during POST -> snapshot facts at click + only apply corrs), Fact shape (corr variants lack fields -> normalize), attemptId (none in current -> add to state + LS saved), sim short-circuit, goldens TDD for corr cases (happy + overdraft), banner bloat risk, error keep-local, LS auto, 100% import reuse via grep later, no new deps. Expose flaws harshly in todo notes or comments. Rebuild superior (as in design.md). Self-crit this tasks list itself (too many? no, small verifiable).
- [x] 1.4 Verify no creep/YAGNI gates pre-impl: `grep -n "auth\|grant\|AccessGrant\|new package\|swr\|retry\|AbortController" /home/nabor/_projects/geohod/quests/platform/frontend/app/quest/QuestPlayerClient.tsx /home/nabor/_projects/geohod/quests/platform/frontend/lib/shared-model.ts 2>/dev/null || echo "0 creep (good)"`; confirm sim toggle + "simulate sync" label stay in current code.

## 2. TDD Skeleton First (RED for Reconnect Cases)

- [x] 2.1 Per design + specs + briefing "goldens + replay are the TDD contract": edit /home/nabor/_projects/geohod/quests/platform/frontend/lib/__tests__/player-replay.test.ts to add reconnect sim harness (after happy facts build): const mockCorr = {type:'balance_corrected', step_position:1, coins_delta:3, reason:'overdraft_on_hint', device_id:'device-a', note:null}; const applied = [...facts, normalizeForTest(mockCorr)]; then asserts on length, projectBalance(applied) === authoritative, projectState, then LS json roundtrip + re-project still authoritative. Also happy reconnect (empty corrs) + edge. Do NOT impl normalize yet (will fail).
- [x] 2.2 Run the test to confirm RED: `cd /home/nabor/_projects/geohod/quests/platform && npx tsx frontend/lib/__tests__/player-replay.test.ts 2>&1 | cat` (expect fail on reconnect corr apply or normalize or post-sync assert; capture output).
- [x] 2.3 Add comment in test: "// TDD RED done for reconnect; will turn green after shared normalize + client apply logic (see tasks 3.x, 4.x). Covers happy-with-gift + overdraft corr from facts-sync spec."

## 3. Shared Model Extension (Additive Fact for Corrections, 100% Reuse)

- [x] 3.1 Edit /home/nabor/_projects/geohod/quests/platform/frontend/lib/shared-model.ts: extend FactType union additively with | 'balance_corrected' | 'version_mismatch_corrected'; add optional fields to interface Fact (reason?: string; expected_snapshot?: string; /* etc for corr shapes from backend Rust */ ) so server JSON roundtrips and appendFact accepts without any. Keep projectBalance (sum any coins_delta) and projectState (specific ifs, tolerant to new types) 100% unchanged in behavior.
- [x] 3.2 Verify reuse + no reimpl: `grep -rn "from ['\"].*shared-model['\"]" /home/nabor/_projects/geohod/quests/platform/frontend/app/quest/ /home/nabor/_projects/geohod/quests/platform/frontend/app/constructor/ --include="*.tsx" | cat`; `grep -n "projectBalance\|projectState\|isAnswerCorrect\|Fact" /home/nabor/_projects/geohod/quests/platform/frontend/lib/shared-model.ts | cat` (must show only imports elsewhere, no local redefs).
- [x] 3.3 Run replay (still RED on corr but shared now parses): `npx tsx frontend/lib/__tests__/player-replay.test.ts 2>&1 | head -20 | cat`.

## 4. Client Wiring (Real Sync Handler in QuestPlayerClient, Small + Narrow per React)

- [x] 4.1 In /home/nabor/_projects/geohod/quests/platform/frontend/app/quest/QuestPlayerClient.tsx: extend PlayerState + initial with attemptId?: string, isSyncing: boolean, syncError: string | null, lastCorrMessage: string | null. Add reducer cases 'setAttemptId', 'setSyncing', 'setSyncResult' (error or corr msg + clear). Use functional updates where possible.
- [x] 4.2 Add const attemptId = ... (demo: `demo-${goldenId}`); on mount/restore: if saved.attemptId use it else set demo value + dispatch restore + setAttemptId; persist in LS useEffect also saves attemptId (extend saved object compatibly).
- [x] 4.3 Implement (or enhance) handleSyncClick = useCallback(async () => { if (simOffline) { /* sim alert or message */ return; } const pending = [...facts]; /* snapshot at click for race safety */ dispatch({type:'setSyncing', v:true}); try { const base = 'http://localhost:8080'; /* demo per design */ const res = await fetch(`${base}/api/attempts/${attemptId}/facts`, {method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({facts: pending, snapshot_id: goldenId})}); if (!res.ok) throw new Error(`HTTP ${res.status}`); const resp: {accepted: Fact[], corrections: Fact[], projected: any} = await res.json(); for (const c of (resp.corrections || [])) { const norm = normalizeCorrToFact(c); appendFact(norm); /* will dispatch append */ } const delta = (resp.corrections || []).reduce((s,c:any)=>s+(c.coins_delta||0),0); dispatch({type:'setSyncResult', error:null, corrMsg: delta!==0 ? `balance corrected by ${delta}` : 'synced' }); /* timeout clear msg */ } catch(e){ dispatch({type:'setSyncResult', error: 'sync failed, still offline (local authoritative)', corrMsg:null}); } finally { dispatch({type:'setSyncing', v:false}); } }, [simOffline, facts, attemptId, goldenId, appendFact]);
- [x] 4.4 Add small normalizeCorrToFact(raw: any): Omit<Fact,'device_id'> { return { type: raw.type, step_position: raw.step_position, coins_delta: raw.coins_delta || 0, note: raw.note ?? null, device_id: raw.device_id || 'demo', submitted_value: raw.submitted_value ?? null, local_is_correct: raw.local_is_correct ?? true, ...(raw.reason && {reason: raw.reason}), ...(raw.expected_snapshot && {expected_snapshot: raw.expected_snapshot}) }; } (keeps appendFact working; called only for corrs).
- [x] 4.5 Wire to banner + render: pass attemptId (debug), pendingCount={facts.length}, isSyncing, corrMessage/lastCorr to <OfflineBanner ... />; update handleSimSync calls or consolidate to handleSyncClick; keep all 4-template handlers, claimGift, feedback, nav, popup, terminal, replay untouched.
- [x] 4.6 Ensure narrow effects/handlers per react.md + derived in render (bal/proj already are); useCallback deps minimal (no facts array identity if possible via ref snapshot); no comps defined inside.
- [x] 4.7 Update page.tsx comment if needed for reconnect note (thin RSC remains).

## 5. Banner Enhancement (Small, Reuse Existing, No Bloat)

- [x] 5.1 Edit /home/nabor/_projects/geohod/quests/platform/frontend/app/quest/OfflineBanner.tsx: extend Props with optional pendingCount?: number, isSyncing?: boolean, corrMessage?: string | null; in render add explicit conds (if isSyncing 'syncing...'; else if (corrMessage) show + auto? via parent; show pending count when >0 && !sim; keep exact checkbox + "simulate sync" label + simOffline text; <30 LOC total).
- [x] 5.2 Verify small + explicit: `wc -l /home/nabor/_projects/geohod/quests/platform/frontend/app/quest/OfflineBanner.tsx`; `grep -n "if\|simOffline\|isSyncing\|corrMessage" /home/nabor/_projects/geohod/quests/platform/frontend/app/quest/OfflineBanner.tsx | cat`.

## 6. LS + State + Error + Sim Preserve + Fidelity

- [x] 6.1 Ensure LS key unchanged (`quest-player-${goldenId}`), payload extend {facts, stepIdx, attemptId, ts} backward compat (load code tolerates missing attemptId).
- [x] 6.2 In client: on real success/error, banner states update (pending visual via count or text); on corr, lastCorrMessage shown briefly (timeout dispatch clear like bonus).
- [x] 6.3 Sim path: if(simOffline) { alert('Simulated... (kept for toggle)'); return; } before any fetch. Toggle still flips simOffline + resets some sync state.
- [x] 6.4 All prior flows (gift auto for pos2, "МИХАЙЛО ПУПИН", popup only on wrong, nav, feedback any-page, complete, ctor link) still work mid or post sync (assert via replay + manual).

## 7. Tests, Gates, Verify, Build

- [x] 7.1 Re-run replay (now should go green on reconnect cases): `cd /home/nabor/_projects/geohod/quests/platform && npx tsx frontend/lib/__tests__/player-replay.test.ts 2>&1 | cat` (PASS all happy + corr + LS + "reconnect" asserts; note goldens fidelity post-corr).
- [x] 7.2 Frontend gates: `cd /home/nabor/_projects/geohod/quests/platform/frontend && npm run build 2>&1 | tail -10 | cat && npm run lint 2>&1 | tail -5 | cat` (clean, no new errors from Fact/additive or fetch).
- [x] 7.3 Reuse/DRY gate: `grep -rn "from ['\"].*shared-model['\"]" /home/nabor/_projects/geohod/quests/platform/frontend/app/quest/ --include="*.tsx" | grep -E "(projectBalance|projectState|Fact|isAnswerCorrect)" | cat` (all via import; 0 local reimpls); also `grep -c "appendFact\|projectBalance" /home/nabor/_projects/geohod/quests/platform/frontend/app/quest/QuestPlayerClient.tsx` (reuse).
- [x] 7.4 Manual "Save + test" verify (per briefing): (assume backend running `cd backend && cargo run` on 8080), from /constructor "Save + open in real player" or direct /quest?golden=mystery-fortress-v1 , play steps (incl gift), toggle sim disconnect + play more, disable + "simulate sync" (now real), assert no loss, bal authoritative, banner update, if force overdraft (edit local or multi sim) see corr apply + "corrected" banner. Reconnect multi times. Capture notes. (If no backend: sim path + replay cover logic.)
- [x] 7.5 Re-verify with real backend if running (per design): `curl -X POST http://localhost:8080/api/attempts/demo-mystery-fortress-v1/facts -H 'content-type: application/json' -d '{"facts":[{"type":"physical_confirmed","step_position":0,"submitted_value":null,"local_is_correct":true,"coins_delta":0,"note":null,"device_id":"local-demo"}],"snapshot_id":"mystery-fortress-v1"}' | cat` (expect accepted/corrections/projected); then player test.
- [x] 7.6 Update landing if needed (status text in /home/nabor/_projects/geohod/quests/platform/frontend/app/page.tsx to mention "real reconnect wired (toggle offline + sync)"): optional drive-by, only if status text references player.
- [x] 7.7 Full openspec + git? (no): `cd /home/nabor/_projects/geohod/quests/platform && openspec status --change "player-reconnect-wiring"` (tasks all done).

## 8. Self-Critique + Final Gates + PROPOSE COMPLETE

- [x] 8.1 Re-apply full cycle to artifacts + wiring: re-read design.md Risks/Self-crit section + proposal Impact; run greps for YAGNI (no auth in code), react compliance (no inside comps, derived render, narrow), 100% shared (imports only). Expose any remaining flaw (e.g. fetch port, minimal banner UX, no retry) and confirm YAGNI per briefing. Rebuild if needed (none). Self-crit tasks: 25+ small, TDD RED first, reads heavy, verifiable cmds, covers edges (race, corr shape, sim, LS, goldens, error, multi-reconnect), no creep.
- [x] 8.2 Final status plain + json: `cd /home/nabor/_projects/geohod/quests/platform && openspec status --change "player-reconnect-wiring" && openspec status --change "player-reconnect-wiring" --json | cat`.
- [x] 8.3 Confirm all applyRequires (tasks) done, artifacts clean (no context/rules leaked: `grep -c "Monorepo root\|agents/react\|YAGNI vs best-practice\|Chief Staff" /home/nabor/_projects/geohod/quests/platform/openspec/changes/player-reconnect-wiring/*.md /home/nabor/_projects/geohod/quests/platform/openspec/changes/player-reconnect-wiring/specs/**/*.md || echo "0 leaks (good)"`).
- [x] 8.4 PROPOSE COMPLETE for player-reconnect-wiring. (N/M tasks; key files: /home/nabor/_projects/geohod/quests/platform/openspec/changes/player-reconnect-wiring/{proposal.md,design.md,specs/quest-player-pwa/spec.md,tasks.md}; frontend will be /home/nabor/_projects/geohod/quests/platform/frontend/app/quest/QuestPlayerClient.tsx + OfflineBanner.tsx + lib/shared-model.ts + lib/__tests__/player-replay.test.ts on apply; summary: wiring closes client/server facts loop per PLAN after backend-facts-sync; goldens (happy "МИХАЙЛО ПУПИН"+5 + overdraft corr) drive TDD reconnect; sim toggle + YAGNI preserved; 100% reuse; small focused; survives skepticism.)

All artifacts + cycle survive adversarial scrutiny. Ready for orchestrator review + apply.
