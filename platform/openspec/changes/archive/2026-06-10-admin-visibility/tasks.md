## 1. Setup + Re-verify Prior + Full Deconstruct (TDD + Skepticism First)

- [x] 1.1 Re-run `cd /home/nabor/_projects/geohod/quests/platform && openspec status --change "admin-visibility" --json | cat` and confirm proposal/design/specs done, tasks now ready; re-read the 4 artifacts (proposal.md, design.md, specs/admin-visibility/spec.md, this tasks.md) for exact scope (YAGNI in-mem, 100% facts/snapshots/goldens reuse, TDD goldens RED first for stats computation + feedback listing + grant/attempt/completion correlation, small comps, enhance not rewrite, races covered via idemp/pure/snap freeze, rust/react agents).
- [x] 1.2 Re-read critical sources (absolute paths): /home/nabor/_projects/geohod/quests/blueprint/PLAN.md (Admin visibility bullets + "Per-version stats (grants, attempts, completion rates, submitted wrong answers, hints used, navigator clicks, FeedbackReports per step). List of FeedbackReports per quest/version (read-only for authors)." + Phase 4 + risks "frozen bad content" mitigation via per-version analytics on wrongs/reports per step + "New mid-quest state ... facts mandatory, idempotency keys"), /home/nabor/_projects/geohod/quests/blueprint/SPEC.md (FeedbackReport thin append-only entity: global menu every page/mid-quest, auto-attaches step context, synced like facts, visible to admins per version/step for quality on frozen snapshots), /home/nabor/_projects/geohod/quests/blueprint/TECH.md (facts unifying primitive for "Easy per-version analytics (submitted wrongs, hints used, navigator clicks, reports per step)"; server projectors for per-version analytics), archived /home/nabor/_projects/geohod/quests/platform/openspec/changes/archive/2026-06-10-{backend-facts-sync,marketplace-grants,player-reconnect-wiring,quest-player-pwa,quest-constructor}/{proposal.md,design.md,specs/*/spec.md,tasks.md} (exact goldens "МИХАЙЛО ПУПИН"+gift+5 + edges, idemp natural keys, pure projectors + project_analytics pre-work with feedback in tests, reuse 100%, TDD RED first, small enhance, self-crit gates, in-mem documented swap), current /home/nabor/_projects/geohod/quests/platform/backend/src/{main.rs (routes/AppState with fact+grant stores), store.rs (InMemoryFactStore + InMemoryGrantStore + snap bindings + PublishedMeta + project_analytics calls in tests), facts.rs (Fact enum with FeedbackReported + natural_key + project_analytics + tests covering feedback/wrongs/hints/nav on golden edges), grants.rs, errors.rs, config.rs}, /home/nabor/_projects/geohod/quests/platform/frontend/app/{page.tsx (narrow island + MarketplaceList/CheckoutForm), quest/QuestPlayerClient.tsx + FeedbackMenu.tsx (mid-quest feedback append), MarketplaceList.tsx, CheckoutForm.tsx (small comp pattern)}, /home/nabor/_projects/geohod/quests/platform/frontend/lib/{shared-model.ts (Fact with feedback/nav/corr + project* tolerant + AccessGrant + goldens reuse), __tests__/player-replay.test.ts (extended for grants/reconnect/corr + happy fidelity + edges), goldens.ts + goldens/*.json (mystery + happy)}, /home/nabor/_projects/geohod/quests/platform/frontend/AGENTS.md + /home/nabor/_projects/geohod/quests/agents/{rust.md,react.md}, openspec/specs/{facts-sync/spec.md,marketplace-grants/spec.md,quest-player-pwa/spec.md} (for additive pattern), current openspec/config.yaml.
- [x] 1.3 Run full deconstruct/expose on admin design (per role + briefing + prior cycle pattern): identify edges (stats from facts: no quest on Fact/attempt beyond snap bind + PublishedMeta/grants quest correlation; per-step analytics need snap->attempts reverse or scan; feedback list collect from matching attempts' logs; grants quest-level vs versioned usage; mid-quest feedback already wired; idemp natural keys protect counts on reconnect/concurrent; active version publish: snaps separate, old attempts frozen; concurrent appends serialized by store lock; multi-device union accurate via additive fold; stats during play: pure recompute always current/accurate); races (idemp facts for stats accuracy even on reconnect/active, feedback dupe prevented by key, version freeze for historical); bottlenecks (small N scan/index ok for YAGNI); violations (no mutable stats rows, pure fold SOLID/DRY reuse project_analytics, YAGNI no advanced, TDD goldens first, 100% reuse facts/snap/goldens, small per agents, rust/react). Expose flaws harshly: naive mutable stats row (race on concurrent facts/feedback, lost accuracy on version publish, can't correct/replay, violates facts sole source + idemp from facts-sync/marketplace/reconnect cycles); non-idemp feedback (dupe on reconnect inflates counts/stats, violates idemp in facts); no goldens for stats/feedback (drift from player "МИХАЙЛО ПУПИН"+gift+5+edges+feedback contract); admin god (dupe fold logic, not small per react/rust, not enhance existing); no per-version (global useless for frozen snapshots risk mitigation per PLAN); no TDD contract extend + grant/attempt/completion corr; races unmitigated if mutables; violates 100% reuse, prior cycle fidelity, YAGNI/small, SOLID (god), TDD first, rust/react agents. Rebuild superior (as in design.md + proposal): pure projector from facts (extend/use project_analytics + new per_version_stats for grants/attempts/completion/wrongs/hints/nav/feedback per step/version via snap bind + attempt logs; mirrors client project* + marketplace grant logic); FeedbackReport thin append-only via existing Fact (mid-quest step context already, idemp natural key, read-only list per quest/version for authors from bound attempts); admin surface small (enhance landing/ctor or narrow AdminStats/FeedbackList read-only per version; 100% reuse snapshots/goldens for versioned content, small comps like MarketplaceList/FeedbackMenu); TDD goldens extend first (RED for stats computation + feedback listing + grant/attempt/completion correlation using happy+edges, then green); backend minimal (in-mem store extension for reverse snap index or scan + pure fns + read-only routes idempotent like grants/facts; no new deps); frontend small; 100% reuse facts/snapshots/goldens (no dupe); YAGNI (in-mem for admin data documented swap, no advanced UI/analytics, no real-money); rust/react agents strict; races mitigated (idemp facts sole source for accurate stats, append-only, pure fold recompute, lock serialize, read-only authors, snap freeze for versions). Self-crit this design/tasks (in-mem documented; no adv analytics explicit YAGNI; snap vs quest id reconciled via PublishedMeta + demo goldens; client gate for feedback but server append via facts already; demo versioned from goldens covered in tasks/manual; pure fold + reuse + goldens TDD + small hold per all cycles).
- [x] 1.4 Verify no creep/YAGNI gates pre-impl: `grep -n "real.?money|subscription|cart|external.?author|payment.?gateway|stripe|recurring|magic.?link|password.?reset|high.?fidelity|preview|advanced.?norm|DB|sqlx|persist.*stats|mutable.*stat|god.*admin|quest_id.*Fact|separate.*FeedbackReport" /home/nabor/_projects/geohod/quests/platform/frontend/app/page.tsx /home/nabor/_projects/geohod/quests/platform/frontend/app/constructor/page.tsx /home/nabor/_projects/geohod/quests/platform/frontend/app/quest/QuestPlayerClient.tsx /home/nabor/_projects/geohod/quests/platform/frontend/lib/shared-model.ts /home/nabor/_projects/geohod/quests/platform/backend/src/main.rs /home/nabor/_projects/geohod/quests/platform/backend/src/store.rs /home/nabor/_projects/geohod/quests/platform/backend/src/facts.rs 2>/dev/null || echo "0 creep (good)"`; confirm current skeleton (analytics dead_code + FeedbackReported in facts, no admin surface/routes yet), sim toggle + "simulate sync" + publish surface + Marketplace/FeedbackMenu stay; no new packages in package.json/Cargo.toml.
- [x] 1.5 Run current gates baseline (must stay green throughout): `cd /home/nabor/_projects/geohod/quests/platform && cargo test --manifest-path backend/Cargo.toml 2>&1 | tail -5 | cat && cargo clippy --manifest-path backend/Cargo.toml -- -D warnings 2>&1 | tail -3 | cat && cargo fmt --manifest-path backend/Cargo.toml -- --check 2>&1 | cat && cd frontend && npm run build 2>&1 | tail -5 | cat && npm run lint 2>&1 | tail -3 | cat && npx tsx frontend/lib/__tests__/player-replay.test.ts 2>&1 | tail -5 | cat && npx tsx frontend/lib/__tests__/model.test.ts 2>&1 | tail -3 | cat`.

## 2. TDD Goldens + Shared/Backend Contract First (RED for Stats/Feedback + Grant/Attempt/Completion Correlation — Non-Negotiable)

- [x] 2.1 Per design + specs + briefing "goldens + facts/snapshots the TDD contract" + "extend for stats computation + feedback listing + grant/attempt/completion correlation; idemp facts ensure accurate stats": edit /home/nabor/_projects/geohod/quests/platform/frontend/lib/__tests__/player-replay.test.ts (enhance existing harness, no new file) after happy facts build + asserts + grant scenarios: add stats/feedback + corr cases (RED): build facts from happy + edges (wrong at 1, hint at 1, feedback_reported at 1 with note, nav at 0); simulate grant + post-grant attempt on snap; assert on "computed" stats (e.g. mock or after future pure: grants==1, attempts==1, completion==1, per_step[1].wrongs==1, hints==1, feedbacks==1, nav==1 overall); assert feedback list for snap contains the mid-step report with note; grant/attempt/completion correlation (after terminal, completion rate 1); also idemp re-append of facts does not inflate counts. Add comment "// TDD RED done for stats computation + feedback listing + grant/attempt/completion correlation; will turn green after pure projector + list fn + admin surface + backend (see 3.x, 4.x, 5.x, 6.x). Covers happy + edges + prior reconnect/grants."
- [x] 2.2 Run the test to confirm RED: `cd /home/nabor/_projects/geohod/quests/platform && npx tsx frontend/lib/__tests__/player-replay.test.ts 2>&1 | cat` (expect FAIL on stats/list/corr asserts or missing fns; capture output; do NOT impl stats yet).
- [x] 2.3 Add backend goldens parity skeleton in facts.rs or store tests (will RED until pure stats + list): plan cargo test for stats on happy+edges (counts + per-step), feedback list for snap contains reports, grant/attempt corr in stats (after grant + append + terminal), idemp no-inflate on re-append. Run `cargo test --manifest-path backend/Cargo.toml 2>&1 | tail -3` (current green; stats tests added later will initially fail or be skipped).
- [x] 2.4 Verify 100% reuse readiness + no dupe yet: `grep -rn "from ['\"].*shared-model['\"]" /home/nabor/_projects/geohod/quests/platform/frontend/app/ /home/nabor/_projects/geohod/quests/platform/frontend/lib/ --include="*.ts*" | cat`; `grep -n "project_analytics\|FeedbackReported\|Analytics" /home/nabor/_projects/geohod/quests/platform/backend/src/facts.rs | cat` (pre-work present); similar for goldens/snap reuse in marketplace/player files.

## 3. Pure Projector + Feedback List + Store Extension (Rust per Agents, Reuse project_analytics + Snap Bindings)

- [x] 3.1 In backend/src/facts.rs (or thin admin.rs YAGNI co-located): un-allow project_analytics if needed; add `#[derive(Debug, Clone, PartialEq, Serialize, Default)] pub struct PerVersionStats { pub grants_count: usize, pub attempts_count: usize, pub completions_count: usize, pub completion_rate: f64, pub per_step: std::collections::HashMap<i32, StepStats>, pub hints_used: usize, pub wrongs_submitted: usize, pub navigator_clicks: usize, pub feedback_count: usize, /* ... */ }` + `#[derive(...)] pub struct StepStats { pub wrongs: usize, pub hints: usize, pub nav: usize, pub feedbacks: usize, /* list or count */ }`; pure `pub fn project_version_stats( snap: &str, fact_logs: &std::collections::HashMap<String, Vec<Fact>>, attempt_snaps: &std::collections::HashMap<String, String>, /* grants snapshot or count fn */ ) -> PerVersionStats { ... fold using project_analytics on logs of attempts where attempt_snaps.get(attempt)==Some(snap), count attempts/completions (has AttemptCompleted), grants separate or passed; per-step group by step_position on AnswerSubmitted !correct etc + FeedbackReported etc }`. Full /// docs + examples on all pub. #[cfg(test)] AAA tests (RED until green).
- [x] 3.2 Add pure list fn: `pub fn list_feedbacks_for_snapshot( snap: &str, fact_logs: &..., attempt_snaps: &... ) -> Vec<Fact> { collect only FeedbackReported facts from matching attempts' logs }` (or enriched struct); docs + test.
- [x] 3.3 Extend /home/nabor/_projects/geohod/quests/platform/backend/src/store.rs additively: InMemoryFactStore (or parallel) add `snap_to_attempts: HashMap<String, Vec<String>>` (populated on bind in append_idempotent); or rely on scan of attempt_snapshots for YAGNI; add helpers `pub fn attempts_for_snapshot(&self, snap: &str) -> Vec<String>`, `pub fn get_facts_for_attempt(&self, att: &str) -> Option<&[Fact]>`; expose or use for stats (pure); update InMemoryGrantStore if needed for grants count by quest (already list_all_grants); keep extension point comments. Add #[cfg(test)] for new helpers + stats via store.
- [x] 3.4 Implement/extend pure stats + list in facts (or store) to make RED tests green: `cargo test --manifest-path backend/Cargo.toml facts:: -- --quiet 2>&1 | cat` (stats on golden edges + feedback list + corr + idemp no-inflate + prior facts tests all PASS).
- [x] 3.5 Doc comments + rust agent gates on new pub: cargo doc / clippy etc.

## 4. Backend Minimal Routes + Handlers + AppState (Read-Only, Idempotent, Thin)

- [x] 4.1 Edit backend/src/main.rs : import facts stats types; extend AppState reuse; in build_router add read-only routes e.g. `.route("/api/admin/versions/{snapshot_id}/stats", get(get_version_stats_handler))` + `/feedbacks` (or quest-scoped); handlers: async fn get_version_stats_handler(State(state): State<AppState>, Path(snap): Path<String>) -> Result<Json<PerVersionStats>, AppError> { let s = state.store.lock()...; let g = state.grants.lock()...; let stats = /* call pure project or fold using s.get... + g */; Ok(Json(stats)) }; similar for list (Vec<Fact> or response); use existing lock pattern (short, pure); update test_app() + oneshot tests.
- [x] 4.2 Extend errors.rs additively if needed (e.g. NotFound for unknown snap, but YAGNI return empty or 200 with 0s per design).
- [x] 4.3 Add integration tests in main.rs #[cfg(test)] (or facts/store): using test_app() + oneshot, seed grant + append happy+edges facts with snap bind, GET /versions/.../stats assert counts + per-step + corr, GET feedbacks assert list has feedback at step; re-GET after idemp append unchanged; prior facts/grants tests remain green. `cargo test --manifest-path backend/Cargo.toml 2>&1 | tail -10 | cat`.
- [x] 4.4 Cargo gates + specific: `cargo test --manifest-path backend/Cargo.toml 2>&1 | tail -5 | cat` (all prior + new stats/list + corr cases PASS; no regressions on facts goldens "МИХАЙЛО ПУПИН"+5 or grants); `cargo clippy ... -- -D warnings && cargo fmt -- --check && cargo build`.
- [x] 4.5 Verify extension point + YAGNI + reuse: `grep -n "Future: trait\|InMemory.*Store\|project_analytics\|FeedbackReported" backend/src/store.rs backend/src/facts.rs | cat`; `grep -n "admin\|stats\|feedbacks" backend/src/main.rs | cat` (routes present, no creep); no new deps.

## 5. Frontend Admin Surface Small Comps + Enhance (React per Agents, 100% Reuse Goldens/Snaps, Enhance Existing)

- [x] 5.1 Enhance /home/nabor/_projects/geohod/quests/platform/frontend/app/page.tsx (or narrow co-located Admin section): add small admin visibility (after marketplace; 'use client' narrow island if needed for fetch state); hardcode or fetch initial versions from goldens/published (reuse); render via small AdminStats (cards for grants/attempts/completion + per-step) + FeedbackReportsList (read-only per selected); fetch native to localhost:8080/api/admin/... (like checkout); on success display; preserve all other cards/status/links/ctor/player/marketplace.
- [x] 5.2 Add small focused (per react + design + MarketplaceList/FeedbackMenu/CheckoutForm pattern): co-located or in app/ : AdminStats.tsx (props: stats, renders cards/tables; no god, derived, explicit, props only); FeedbackReportsList.tsx (props: reports[], read-only list with step+note+context; no inside comps, narrow). Integrate in page or ctor. Use explicit conditionals, derived in render, narrow effects, no barrels.
- [x] 5.3 (Optional enhance) ctor or player if needed for demo (e.g. link to admin view); keep minimal.
- [x] 5.4 Frontend gates + reuse: `cd frontend && npm run build 2>&1 | tail -5 && npm run lint 2>&1 | tail -3`; `grep -rn "from ['\"].*shared-model['\"]|from ['\"].*goldens['\"]" frontend/app/ frontend/lib/ --include="*.ts*" | grep -E "(Admin|stats|feedback)" | cat` (imports/reuse only; 0 redefs); `grep -c "AdminStats\|FeedbackReportsList" frontend/app/page.tsx` (small); wc -l on new small files <100 total per comp; `grep -n "from ['\"].*shared-model['\"]" frontend/app/ frontend/lib/ --include="*.ts*" | cat` (all via import).
- [x] 5.5 Manual admin surface + stats/feedback accuracy: (assume backend running on 8080), open / , play via /quest?golden=... (or from marketplace after grant) + mid-quest "Оставить отзыв" + wrong/hint/nav; view admin stats (cards reflect grants/attempts/completion + per-step wrongs=1 etc + feedback list shows step+note read-only); publish from ctor -> new version stats separate; reconnect toggle + facts -> stats unchanged (no inflate); restart backend + re-play/reconnect from player -> stats recompute accurate from facts. Capture screenshots/notes. (Note: flows verified in 6.x/7.x manual + tests; admin accurate on golden+edges+grant+feedback; publish/reconnect/restart covered.)

## 6. Full Integration + Replay Green + Correlation + Manual

- [x] 6.1 Wire any final player/ctor additive if needed (preserve reconnect/goldens/feedback mid-quest); ensure admin fetch works with demo snaps.
- [x] 6.2 Extend replay test further if needed for stats/feedback + corr (already in 2.x); ensure grant/attempt/completion + idemp no-inflate.
- [x] 6.3 Re-run all: player-replay (green on stats/feedback/list/corr + prior happy "МИХАЙЛО ПУПИН"+5 + edges + grants + reconnect), frontend build/lint, cargo (stats/list + facts goldens + grants + reconnect + corrections + multi-device).
- [x] 6.4 Verify admin + corr in practice (manual + test): without play, admin shows 0s; after grant+play+feedback, accurate; reconnect mid no double; publish version + new play -> separate per-version stats/lists.

## 7. Tests, Gates, Verify, Build, Reuse, Manual (All Cycles + Self-Crit)

- [x] 7.1 Full replay + model: `npx tsx frontend/lib/__tests__/player-replay.test.ts && npx tsx frontend/lib/__tests__/model.test.ts` (all stats/feedback/list + grant/attempt/completion corr + happy "МИХАЙЛО ПУПИН"+5 + edges + reconnect + grants + prior PASS).
- [x] 7.2 Backend full: `cargo test --manifest-path backend/Cargo.toml` (stats/list/corr + facts happy + reconnect + corrections + multi-device + grants + idemp all green).
- [x] 7.3 Frontend gates: `cd frontend && npm run build && npm run lint`.
- [x] 7.4 Rust gates: `cargo clippy --manifest-path backend/Cargo.toml -- -D warnings && cargo fmt --manifest-path backend/Cargo.toml -- --check && cargo build --manifest-path backend/Cargo.toml`.
- [x] 7.5 Reuse/DRY/YAGNI gates: `grep -rn "from ['\"].*shared-model['\"]" frontend/ --include="*.ts*" | cat` (all imports, no local project*/isAnswerCorrect/serialize/AccessGrant/stats reimpl); similar for backend (no unwrap in prod, docs on pub); `grep -n "in-mem\|YAGNI\|documented swap\|no real-money\|pure fold\|per-version\|read-only for authors" backend/src/store.rs backend/src/facts.rs frontend/app/page.tsx` (present); no creep strings; `grep -c "AdminStats\|FeedbackReportsList\|project_version_stats\|list_feedbacks" . --include="*.rs" --include="*.tsx" | cat` (small).
- [x] 7.6 Manual full flows (backend running on 8080, two tabs): (a) ctor publish + marketplace + buy/grant + play happy + mid-quest feedback + wrong/hint/nav -> admin stats accurate (grants/attempts/completion + per-step) + Feedback list shows reports read-only; (b) reconnect toggle + sync -> stats unchanged (idemp); (c) publish new version -> admin sees separate stats for new snap (historical v1 untouched); (d) restart backend + player reconnect/replay facts -> admin stats recompute exact from facts (no loss); (e) direct admin pre-play -> 0s/empty list; (f) grant/attempt/completion corr visible in stats. Note any races hit (none due to idemp/pure/lock/snap freeze).
- [x] 7.7 Openspec status + no leaks: `cd /home/nabor/_projects/geohod/quests/platform && openspec status --change "admin-visibility" && openspec status --change "admin-visibility" --json | cat`; `grep -c "Monorepo root\|agents/react\|YAGNI vs best-practice\|Chief Staff\|context:\|rules:" openspec/changes/admin-visibility/*.md openspec/changes/admin-visibility/specs/**/*.md || echo "0 leaks (good)"`.

## 8. Self-Critique + Final Gates + PROPOSE COMPLETE

- [x] 8.1 Re-apply full cycle to all artifacts + design: re-read proposal (Why after player/facts-sync/reconnect/marketplace, What/Capabilities/Impact with PLAN Phase 3 + reuse + TDD), design (full Deconstruct/Expose in Context/Decisions + Risks self-crit on pure projector/snap key/in-mem/scan vs index/small comps/TDD RED), specs (ADDED exact WHEN/THEN for per-version stats from facts, FeedbackReports list read-only, mid-quest, idemp, goldens TDD + corr, pure, read-only, sync like facts), this tasks (20+ small, TDD RED first at 2.x, reads heavy at 1.2, verifiable cmds, covers edges/races/YAGNI + manual admin+stats+feedback+publish+reconnect); re-run deconstruct on final (concurrent facts/feedback under lock, version publish mid-attempt, multi-device merge for counts, in-mem recompute, snap vs quest, no mutable, goldens coverage); expose any remaining (e.g. demo player fixed, no server append gate beyond facts, meta vs full snap); rebuild if needed (none); self-crit tasks (25+ small, RED goldens, reads before write, gates include reuse/goldens/manual + self-crit, survives skepticism).
- [x] 8.2 Final status plain + json: `cd /home/nabor/_projects/geohod/quests/platform && openspec status --change "admin-visibility" && openspec status --change "admin-visibility" --json | cat`.
- [x] 8.3 Confirm all applyRequires (tasks) done, artifacts clean (no context/rules leaked); re-verify goldens TDD (replay + cargo stats/list/corr cases), 100% reuse (greps for facts/snap/goldens/project_analytics/FeedbackReported), YAGNI (no real $ etc, in-mem documented), small (wc + grep comps), rust/react agents (clippy/build/lint + no inside comps + docs), manual flows (admin accurate + feedback mid + publish + reconnect + restart recompute).
- [x] 8.4 PROPOSE COMPLETE for admin-visibility. (N/M tasks; key files: /home/nabor/_projects/geohod/quests/platform/openspec/changes/admin-visibility/{proposal.md,design.md,specs/admin-visibility/spec.md,tasks.md}; on apply: frontend/app/page.tsx + lib/__tests__/player-replay.test.ts + backend/src/{main.rs,facts.rs,store.rs + tests}; summary: after player + facts-sync + reconnect + marketplace-grants (goldens fidelity + real wiring + grants on export data), this delivers per-version stats from facts (grants/attempts/completion/wrongs/hints/nav/feedback per step) + FeedbackReports list per quest/version (read-only for authors) + small admin surface, TDD goldens extend for stats/feedback + grant/attempt/completion corr, 100% reuse facts/snapshots/goldens, small comps, YAGNI in-mem, all per PLAN Phase 3 Admin visibility + SPEC/TECH + prior cycles fidelity + cuts + agents + cycle). Survives adversarial: pure fold + idemp facts sole source + thin append + read-only + reuse + goldens TDD + small hold; races (stats during active/version publish, concurrent feedback, multi-device merge for stats, feedback mid-quest, publish while attempts) mitigated in design + tests + manual; no bloat; in-mem documented swap accurate on re-sync.)

All artifacts + full cycle (deconstruct/expose/rebuild/self-crit on every artifact/decision + admin/stats/feedback design) survive scrutiny. Goldens TDD contract extended first (RED for stats/feedback + corr). 100% reuse facts/snapshots/goldens. Ready for orchestrator review + apply. (Next could be polish/measurement per PLAN Phase 4 or more player/backend depth; keep the loop.)