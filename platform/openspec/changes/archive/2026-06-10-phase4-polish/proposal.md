## Why

After the quest-player-pwa + backend-facts-sync + player-reconnect-wiring + marketplace-grants + admin-visibility cycles (4-template player with any-page feedback + goldens fidelity on real export "МИХАЙЛО ПУПИН"+gift+5+edges/wrongs/hints/feedback/nav + real reconnect wiring + lifetime idemp source-audited grants + peer marketplace + checkout + 100% coupon + free identical + 100% shared + TDD goldens + enhance existing + YAGNI + per-version stats/feedback from facts pure + read-only admin surface for authors), Phase 4 "Polish, Migration, Measurement" per PLAN is required to close the loop. Ctor import/export (YAML preferred) as power path for bulk ops and migration; per-version analytics surfaced in ctor and admin views; migration job (one-time, idempotent): historical grants + answer_card data → synthetic legacy snapshots + facts. Audit diffs vs old scalars. Mark imported items; measure (post-MVP where possible): real bundle sizes with actual comics + audio + nav data; ctor velocity and defect rate with new visual/UX fields and gates; sync correction rate; player feedback on any-page "Оставить отзыв", navigator button, popup hint UX, animated bonuses; abuse patterns on rating spend (if enabled); update invariants and 08_DECISIONS_LOG with any new learnings. Re-run adversarial review if client data or real quest exports reveal new edges. Reuses facts/snapshots/goldens; TDD goldens for migration/measurement flows; small comps; rust/react agents. This makes bulk authoring/migration real, surfaces usage analytics for frozen snapshots (mitigates bad-content risk), reconciles legacy data accurately via facts (idemp + audit), and measures post-MVP fidelity/UX without bloat.

## What Changes

- Ctor import/export tab (YAML preferred): YAML/JSON paste or file → strict parse/validate (via shared model + goldens loaders + validateForPublish) → load into sortable step list; "Export current as YAML" (roundtrippable); power path for bulk ops and future migration of legacy content.
- Per-version analytics surfaced in ctor and admin views (reuse existing admin surface + ctor patterns; pure from facts + grants + snap bindings; grants/attempts/completions + per-step wrongs/hints/nav/feedback; read-only for authors).
- Migration job (one-time, idempotent): historical grants + answer_card data → synthetic legacy snapshots (at export-time content via goldens/snap structure) + facts (StepCompletion + Coin + Feedback etc); audit diffs vs old scalars (pure projector compare, manual review output); mark imported items (e.g. via source/flag/note on synth grants/facts).
- Measure (post-MVP where possible): real bundle sizes (actual comics + audio + nav data from exports/goldens + est gate); ctor velocity and defect rate (with new visual/UX fields + gates); sync correction rate (from corr facts); player feedback on any-page "Оставить отзыв"/navigator button/popup hint UX/animated bonuses (via existing FeedbackReport + usage analytics); abuse patterns on rating spend (if enabled, via facts fold); update invariants and 08_DECISIONS_LOG with learnings; re-run adversarial if new edges from real data.
- TDD goldens contract extended first (RED for migration flows: historical→synth legacy snaps+facts, audit diffs==expected, mark; + measurement flows: bundle/ctor vel/corr rate/fb/abuse + update log; then green); 100% reuse facts/snapshots/goldens (no dupe logic for synth/parse/project/audit).
- Backend minimal (in-mem or simple extension for migration marks/synth data + measurement; idempotent pure fns like grants/facts; thin read/job handlers; documented swap path).
- Frontend small (enhance existing ctor import/export stub + admin surface in page/ctor for analytics + narrow migration/measurement UI; small comps per react agents; 100% reuse goldens/shared for snaps/versioned).
- Strict YAGNI (in-mem for migration data, basic measurement no advanced UI/instrumentation, no real persist yet, enhance not rewrite); no **BREAKING** (additive only; all prior goldens/facts/grants/player/ctor flows + fidelity untouched).
- Follows TDD (goldens non-negotiable first), SOLID/DRY/KISS/YAGNI (pure reuse, small enhance, idemp), rust/react agents, blueprint PLAN Phase 4 + SPEC/TECH (import/export, per-ver analytics from facts, migration synth+audit+mark, post-MVP meas, update 08/invariants) + archived artifacts fidelity (reuse facts/snap/goldens exactly).

## Capabilities

### New Capabilities
- `phase4-polish`: Ctor import/export (YAML preferred) as power path for bulk ops and migration (paste/file strict parse/validate/load + export YAML); per-version analytics surfaced in ctor and admin views (pure fold from facts/grants/snap bindings, reuse surface); migration job (one-time idempotent): historical grants + answer_card → synthetic legacy snapshots + facts, audit diffs vs old scalars, mark imported items; measure (post-MVP): real bundle sizes with comics/audio/nav, ctor velocity/defect with visual/UX/gates, sync corr rate, player feedback on "Оставить отзыв"/navigator/popup/animated bonuses, abuse on rating spend (if), update invariants/08_DECISIONS_LOG + re-run adv review on new edges from data; TDD goldens extended for migration/measurement flows; 100% reuse facts/snapshots/goldens; small comps + minimal backend/frontend + YAGNI in-mem/basic; rust/react agents; survives races via idemp/pure/snap freeze/goldens TDD. (Creates `specs/phase4-polish/spec.md`)

### Modified Capabilities
(none; existing quest-constructor / admin-visibility / facts-sync / marketplace-grants / quest-player-pwa REQUIREMENTS unchanged — ctor import/export already declared in quest-constructor spec and stub present (enhance for YAML/strict); analytics additive surface in existing admin/ctor; migration/measurement additive pure jobs/fns over existing facts/grants/snap/goldens; no behavior change to append/idemp/player/grants/publish/snap freeze)

## Impact

- **Frontend**: `frontend/app/constructor/page.tsx` (enhance existing Import/Export stub section for full YAML preferred + strict parse/validate via shared + load to list + export YAML; surface per-version analytics cards/list reuse goldens/snap for versioned); `frontend/app/page.tsx` (enhance admin surface + narrow ctor analytics surface; 100% reuse goldens/shared for snaps + small comps modeled on MarketplaceList/FeedbackReportsList/AdminStats); `frontend/lib/__tests__/player-replay.test.ts` + goldens (extend for RED migration synth/audit/mark + measurement rates/bundle/ctor/fb/abuse + update log cases, then green; happy "МИХАЙЛО ПУПИН"+gift+5 + edges + prior grants/reconnect/admin untouched); additive only, no new files preferred.
- **Backend**: `backend/src/facts.rs` (additive pure fns e.g. synthesize_legacy_snapshot_facts, audit_diffs_vs_scalars, project_migration_stats or reuse/extend project_analytics/project_version_stats; Fact variants tolerant for legacy synth); `backend/src/store.rs` (additive InMemory* extension for imported_marks or simple map + synth data; helpers for migration job; explicit "Future: trait" like facts/grants); `backend/src/main.rs` (additive read-only/job routes e.g. /api/migrate/legacy (idemp one-time), /api/measure/* basic; thin handlers reuse AppState locks + pures; test_app updates); `backend/src/grants.rs` (tolerant for legacy source mark); integration tests in #[cfg(test)] for migration idemp + audit pass on mock historical + goldens synth facts fidelity + meas rates from facts; no new Cargo deps.
- **Tests/goldens**: Extend replay + backend facts/store tests with migration/measurement cases (RED first on synth legacy from grants+answer_card mocks, audit diffs, marks, bundle ests, rates, fb/abuse, 08 update note); all prior goldens fidelity preserved exactly.
- **OpenSpec + quality**: New `proposal.md` + `design.md` + `specs/phase4-polish/spec.md` + `tasks.md`; on apply syncs to `openspec/specs/`. 20+ small TDD tasks (heavy reads of archived+current+PLAN/SPEC/TECH+goldens first, TDD goldens RED first for mig/meas flows, enhance ctor stub + analytics surface + migration job + basic meas, reuse greps, rust/react gates, cargo/frontend build/lint, manual "import/export YAML" + "run migration job + audit" + "measure bundle/ctor vel/sync corr/player fb/abuse" + "update invariants/08_DECISIONS_LOG" + "re-run adversarial review").
- **Downstream**: Enables bulk/migration power path + per-version analytics for authors (in ctor too) + legacy reconciliation (idemp + audit accurate) + post-MVP measurement + invariants update per PLAN Phase 4 after Player + Sync + Commerce + Admin; mitigates frozen content + migration accuracy risks; all invariants (facts sole source, snapshots frozen, client auth, idemp) + prior cycle fidelity preserved; additive, no breaking.
- Follows TDD (goldens non-negotiable), SOLID/DRY/KISS/YAGNI (pure reuse, small enhance ctor stub/admin, in-mem documented, enhance+small), agents (rust.md: docs/Result/AAA/4sp/no unwrap/clippy; react.md: RSC, small comps, derived, narrow, explicit conds, no barrels), blueprint (PLAN Phase 4 bullets verbatim + "Update invariants and 08_DECISIONS_LOG... Re-run adversarial...", SPEC/TECH migration notes + ctor import/export + per-ver analytics from facts + meas), archived (reuse facts/snap/goldens 100% like admin/marketplace/facts did; TDD RED first, idemp source, self-crit gates). All client/server contracts survive races (migration idemp via marks/pure synth no dupe on re-run/publish/attempts; audit pure/accurate; meas from facts always current; version publish mid unaffected as historical synth separate; multi-device union via idemp; long offline via reconnect facts).

This change makes ctor import/export (YAML power), per-version analytics (ctor+admin), migration job (idemp historical→synth+facts+audit+mark), and post-MVP measurement real and verifiable against the facts/snapshots/goldens contract after the prior cycles. All artifacts produced via full adversarial cycle (deconstruct/expose/rebuild/self-crit on every artifact/decision). Ready for orchestrator review + apply.
