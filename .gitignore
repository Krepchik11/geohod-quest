# Python venv
.venv/

# Secrets
discovery/config/app.env
.env
.env.*
# …but `.env.example` is a committed template (no real secrets), not a secret.
!.env.example

# Raw data (may contain PII)
discovery/raw/data-api/**/list_page_*.json
discovery/raw/exports/

# Logs
discovery/logs/
*.log

# Checkpoints with potentially sensitive metadata
discovery/checkpoints/