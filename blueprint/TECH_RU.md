ТЕХ

Снапшоты:
- явная публикация = неизменяемая версия
- попытка привязана к версии
- исторические сохранять для активных попыток re-dl

События:
- весь прогресс/монеты = только append факты
- AttemptFact + CoinFact
- клиент локальный лог + проекция оффлайн
- синх = append pending + pull + re-project + коррекции

PWA оффлайн:
- самодостаточный бандл ~5mb
- полная игра (шаги, попап подсказки, нав, монеты, подарки)
- локальная валидация + проекция

Клиент vs сервер:
- клиент авторитетен для своего снапшота
- сервер только записывает факты. Без re-val корректности

Multi device:
- факты union по ключу
- детерминированная проекция

Коммерция:
- пожизненный грант идемпотент
- single quest v1
- события для грантов

Конструктор:
- lean форма+список
- immediate structured: AnswerListEditor + live match test (exact клиент fn)
- 4 шаблона пикер
- зоны комиксов по ролям
- конфиг нав
- флаги аним/голос
- wiring попап подсказки
- визуальные гейты + pre publish чеклист
- publish = точная форма бандла
- test в реальном плеере ок

Миграция:
- синтетические legacy снапшоты
- факты из старых данных
- one time идемпотент job